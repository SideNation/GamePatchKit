# <TargetName> 리팩터링 계획

> `csharp-refactoring-architect` 스킬이 복잡한 리팩터링에서만 생성하는 계획 문서. 단일 파일·작은 클래스 리팩터링은 대화 내 계획으로 충분하다.

## 1. 리팩터링 목표

<무엇을 왜 정리하는지 1~3문장. 기능 추가나 버그 수정이 아니어야 한다.>

## 2. 대상 범위

- 포함:
  - `<path/to/File.cs>` — <변경 이유>
- 제외:
  - `<path/to/Other.cs>` — <제외 이유>

## 3. 동작 보존 기준

- Public API:
  - <변경 없음 / 유지할 시그니처>
- 입력/출력:
  - <변경 없음 / 유지할 계약>
- 예외:
  - <type/message 변경 없음 등>
- 저장소/쿼리:
  - <schema, query 결과 변경 없음>
- HTTP/serialization:
  - <status code, response body, DTO shape 변경 없음>

## 4. 현재 구조 요약

<현재 책임 배치, 중복, 의존성, 테스트 상태를 짧게 요약한다.>

## 5. 변경 계획

1. <작은 변경 1> → verify: <확인 방법>
2. <작은 변경 2> → verify: <확인 방법>
3. <작은 변경 3> → verify: <확인 방법>

## 6. 제외 범위

- <요청과 직접 연결되지 않아 건드리지 않을 코드>
- <발견했지만 삭제하지 않을 기존 dead code>
- <이번 리팩터링에서 하지 않을 기능/버그 수정>

## 7. 테스트/검증 계획

- 변경 전:
  - `<dotnet test ...>` — <기준 확인>
- 변경 후:
  - `<dotnet build ...>` — <컴파일 확인>
  - `<dotnet test ...>` — <회귀 확인>
- 실행하지 않는 검증:
  - <명령> — <이유와 잔여 위험>

## 8. 위험과 롤백 기준

- 위험:
  - <예: Repository query shape 변경 가능성>
- 중단 기준:
  - <예: public contract 변경이 필요해지는 경우 사용자 확인>
- 롤백 기준:
  - <예: 관련 테스트 실패 원인이 리팩터링 변경으로 확인되면 해당 단계 되돌림>

## 9. 완료 후 검증 결과

- 변경 요약:
  - <파일별 핵심 변경>
- 동작 보존 근거:
  - <테스트/코드 검토 근거>
- 검증 결과:
  - `<명령>` — <성공/실패/미실행>
- 잔여 위험:
  - <없음 / 남은 위험>
