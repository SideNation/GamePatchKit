# 2026-07 작업 일지

## 📋 2026-07-25 작업 일지 (작성자 → 다음 세션 전달용)

### ✅ 수정/구현: release 식별 계약 단순화

**배경:** `releaseId` 계산 입력에서 manifest schema version이 빠지면 같은 불변 경로에
서로 다른 manifest byte가 충돌할 수 있었다.

**수정:**

- `releaseId`를 제거하고 canonical manifest 원본 byte의 SHA-256인 `manifestHash`를
  불변 manifest 식별자로 사용한다.
- manifest 내부에는 `manifestHash`를 기록하지 않으며 channel과 Runtime current
  pointer가 `manifestHash`를 보유한다.
- `compactVersion`은 최초 package `0`, incremental 상속, compact 시 `+1`로 정의한다.
- 사용자의 명시적 지시에 따라 PRD는 `개발 v2`를 추가하지 않고 기존 `개발 v1` 본문을
  직접 갱신했다.

**파일:** `docs/prd/game-patch-kit-prd.md`, `docs/plan/*.md`

### 🔴 미완료 / 다음 창에서 이어갈 일

- 리뷰 사항 2의 incremental artifact 재사용 조건은 아직 문서에 반영하지 않았다.

### ✅ 수정/구현: incremental artifact 재사용 조건 명확화

**배경:** 경로와 `fileHash`만 같은 기존 bundle 참조를 group·`artifactMode` 변경 뒤에도
재사용하면 file group과 bundle group 경계가 깨질 수 있었다.

**수정:**

- 기존 file artifact는 group·`artifactMode`·compression 설정이 바뀌어도 file
  override로 재사용한다.
- 기존 bundle은 group이 유지되고 현재 `artifactMode`도 `bundle`일 때만 재사용하며,
  그 외에는 file artifact override로 전환한다.
- compression은 새 artifact 생성 정책으로 한정하고, 설정이 바뀌어도 기존 artifact의
  payload와 실제 compression metadata를 그대로 재사용한다.
- 같은 release 안의 혼합 compression과 재사용 codec 지원 요구를 PRD·Runtime·문서화
  계획에 반영했다.

**파일:** `docs/prd/game-patch-kit-prd.md`, `docs/plan/02-schema-canonicalization.md`,
`docs/plan/03-core-identity-diff-plan.md`, `docs/plan/05-packager-file-artifacts.md`,
`docs/plan/06-bundle-compact.md`, `docs/plan/08-runtime.md`,
`docs/plan/13-documentation.md`, `docs/plan/README.md`

### 🗒️ 메모

- 위의 “리뷰 사항 2 미반영” 항목은 이번 작업으로 해결됐다.

## 📋 2026-07-26 작업 일지 (작성자 → 다음 세션 전달용)

### ✅ 수정/구현: optional group과 `PackageState` 계약

**배경:** 최초 plan에서 optional group을 다운로드하지 않으면서도, 전역 release 갱신과
나중의 optional 설치가 어떤 manifest를 기준으로 동작하는지 로컬에 일관되게 기록할
명세가 필요했다.

**수정:**

- 최초 설치와 전역 release 갱신은 required group만 다운로드하고, optional group은 현재
  active manifest를 기준으로 요청 시 별도 설치한다.
- package별 `PackageState` 하나에 전역 active pointer와 group별
  `ready`·`notInstalled`·`stale` 상태를 기록한다.
- 변경되지 않은 optional group은 다운로드 없이 새 active manifest에 재연결하고,
  변경된 optional group은 기존 byte를 보존하되 `stale`로 만들어 노출하지 않는다.
- `PackageState` v1 필드 형식과 불변 조건, opaque `installationKey`, 최초 state 구성,
  writer lock, immutable installation과 원자적 state 교체 순서를 명시했다.
- 손상된 state는 cache 경로만 보고 활성 버전을 추정하지 않으며, host가 제공한 신뢰
  가능한 target pointer를 기준으로만 재구성한다.
- PRD 검증 기준 22·23과 Runtime·DotNet·adapter conformance·문서화 계획을 연결했다.
- 사용자의 명시적 지시에 따라 새 `개발 v2`를 만들지 않고 `개발 v1`을 직접 수정했다.

**파일:** `docs/prd/game-patch-kit-prd.md`,
`docs/plan/03-core-identity-diff-plan.md`, `docs/plan/07-cli.md`,
`docs/plan/08-runtime.md`, `docs/plan/09-dotnet-adapter.md`,
`docs/plan/10-adapter-conformance.md`, `docs/plan/13-documentation.md`,
`docs/plan/README.md`

### ✅ 수정/구현: multi-group activation batch 원자성

**배경:** 여러 group을 한 요청으로 다운로드할 때 group마다 `PackageState`를 commit하면
일부 group만 `ready`인 중간 상태가 노출될 수 있었다.

**수정:**

- 한 Runtime 요청의 target group 집합을 하나의 activation batch로 정의했다.
- group별 다운로드·검증·staging은 병렬로 허용하지만 committed state는 변경하지 않는다.
- 모든 group의 immutable installation 승격 후 `PackageState` 전체를 한 번만 교체하고
  `stateRevision`도 한 번만 증가시킨다.
- 한 group 실패 시 batch 전체가 이전 state를 유지하며, commit 직전
  `active.manifestHash`·`stateRevision` 충돌 시 최신 state 기준으로 재계획한다.
- Runtime·DotNet·adapter conformance에 부분 활성화 방지와 단일 commit 검증을 추가했다.

**파일:** `docs/prd/game-patch-kit-prd.md`, `docs/plan/08-runtime.md`,
`docs/plan/09-dotnet-adapter.md`, `docs/plan/10-adapter-conformance.md`,
`docs/plan/13-documentation.md`

### ✅ 수정/구현: `gamepatchkit.yml` 입력 범위와 schema 역할

**배경:** 사용자 입력 설정, 설정 검증 schema와 Packager 생성 manifest의 역할이
혼동될 수 있었고 YAML 고급 기능의 지원 범위가 정해지지 않았다.

**수정:**

- `gamepatchkit.yml`은 사용자가 작성하는 설정, `package-config.schema.json`은 YAML
  파싱 결과를 검증하는 versioned 배포 계약, release manifest는 Packager가 생성하는
  canonical JSON으로 구분했다.
- YAML은 정확히 하나의 non-empty document와 mapping root만 허용한다.
- anchor·alias·merge key·custom tag·중복 mapping key와 다중 document를 거부한다.
- YAML 문법 검증 → JSON-compatible 데이터 변환 → schema 검증 → 모델 의미 검증
  순서를 고정했다.
- PRD 검증 기준 24와 Schema·CLI·문서화 계획을 연결했다.

**파일:** `docs/prd/game-patch-kit-prd.md`,
`docs/plan/02-schema-canonicalization.md`, `docs/plan/07-cli.md`,
`docs/plan/13-documentation.md`, `docs/plan/README.md`

### ✅ 수정/구현: manifest 계약·canonical golden vector

**배경:** 최상위 manifest 필드와 반복 직렬화 결정성만으로는 file·parts·bundle 구조,
참조 무결성과 서로 다른 구현체의 byte 호환성을 보장할 수 없었다. Adapter conformance의
signed fixture와 서명 구현 단계 사이에도 선행 관계가 맞지 않았다.

**수정:**

- `release-manifest.schema.json`에 file·bundle, single·parts payload와
  file·bundleEntry source의 discriminated union 계약을 추가했다.
- JSON Schema 구조 검증, Core `ManifestValidator` 의미·참조 검증, Packager·Runtime의
  실제 payload byte 검증을 분리했다.
- manifest 내부의 중복·미참조 artifact는 거부하되 공유 저장소의 과거 artifact 파일은
  허용하도록 구분했다.
- RFC 8785 JCS·I-JSON과 domain 배열 정렬, safe integer, hash·base64url encoding을
  고정하고 `manifest-signature.schema.json`을 추가했다.
- single file·multipart file·bundle entry의 공용 golden vector에서 canonical
  identity·manifest byte, `dataVersion`, `manifestHash`와 signature를 고정하도록 했다.
- 10단계는 unsigned conformance, 11단계는 signed extension을 담당하고 12단계는
  `05~11` 이후 실행하도록 의존 관계를 정리했다.
- PRD 검증 기준 25와 Core·Packager·Runtime·conformance·서명 계획을 연결했다.

**파일:** `docs/prd/game-patch-kit-prd.md`,
`docs/plan/02-schema-canonicalization.md`,
`docs/plan/03-core-identity-diff-plan.md`,
`docs/plan/05-packager-file-artifacts.md`, `docs/plan/06-bundle-compact.md`,
`docs/plan/07-cli.md`, `docs/plan/08-runtime.md`,
`docs/plan/10-adapter-conformance.md`, `docs/plan/11-signing-key-rotation.md`,
`docs/plan/12-performance-validation.md`, `docs/plan/13-documentation.md`,
`docs/plan/README.md`

### ✅ 수정/구현: deterministic glob·source snapshot 계약

**배경:** `include`·`exclude`의 glob dialect와 숨김 파일 처리, Core와 Packager의
filesystem 책임 경계가 고정되지 않아 OS·구현체별 파일 선택 결과가 달라질 수 있었다.

**수정:**

- v1 glob을 literal·segment 내부 `*`·완전한 segment `**`만 지원하는 자체 dialect로
  제한하고 `/`, NFC, ordinal case-sensitive matching을 고정했다.
- include OR → exclude 우선 → group matching 순서와 re-include 없음, 숨김 segment의
  명시적 literal `.` 포함 조건을 정의했다.
- editor metadata와 임시 파일의 암묵적 자동 제외를 제거하고 설정의 명시적
  `exclude`로 처리하게 했다.
- Core는 문자열 경로 정규화·glob matching·선택·정렬만 담당하고, Packager는
  symlink·junction·reparse point no-follow 열거와 filesystem 검증을 담당하게 했다.
- Packager가 source identity·크기·수정 시각 snapshot, stream 전후 검증과 최종
  재열거를 수행하고 경합 감지 시 manifest를 만들지 않도록 했다.
- PRD 검증 기준 26과 Schema·Packager·문서화 계획을 연결했다.

**파일:** `docs/prd/game-patch-kit-prd.md`,
`docs/plan/02-schema-canonicalization.md`,
`docs/plan/05-packager-file-artifacts.md`, `docs/plan/13-documentation.md`,
`docs/plan/README.md`

### ✅ 수정/구현: compact no-op·Packager API·서명 rotation·메모리 gate

**배경:** 물리 배치가 같은 compact도 새 manifest를 만들 수 있었고, verify·sign이 CLI
내부 구현으로 굳어질 위험이 있었다. 단일 `manifest.sig`의 rotation 의미와 성능 회귀의
수치 기준도 확정되지 않았다.

**수정:**

- candidate 물리 배치가 source와 같으면 compact를 성공 no-op으로 처리해 기존
  `dataVersion`·`compactVersion`·`manifestHash`를 재사용하고 파일을 만들지 않게 했다.
- Packager에 CLI 독립 streaming verify·sign API와 typed result를 두고 CLI는 키·option·
  출력 adapter만 담당하게 했다.
- `keyId`를 raw Ed25519 public key SHA-256 fingerprint로 고정하고, 기존
  `manifest.sig` 재서명을 금지하며 새 release부터 새 key를 쓰는 rotation 순서를
  정의했다.
- RFC 8032 Section 7.1 known-answer vector와 signature negative test를 완료 기준에
  추가했다.
- Ubuntu 24.04 x64 기준 1GiB fixture peak RSS 512MiB, 256MiB→1GiB 증가 64MiB를
  blocking 성능 gate로 고정했다.
- 기존 검증 기준 9·17·25를 보강하고 새 번호는 추가하지 않았다.

**파일:** `docs/prd/game-patch-kit-prd.md`,
`docs/plan/02-schema-canonicalization.md`,
`docs/plan/03-core-identity-diff-plan.md`,
`docs/plan/06-bundle-compact.md`, `docs/plan/07-cli.md`,
`docs/plan/11-signing-key-rotation.md`, `docs/plan/12-performance-validation.md`,
`docs/plan/13-documentation.md`, `docs/plan/README.md`

### ✅ 구현: 02단계 JSON Schema·canonicalization 전체 구현

**배경:** 계획 문서(02)만 있고 실제 schema·Core 코드는 없었다. 이번 세션에서
계획의 모든 작업 항목을 구현하고 테스트로 검증했다.

**결정 사항 (계획 문서 "결정 사항" 절에도 기록):**

- JSON 파싱 라이브러리: **Newtonsoft.Json**(최소 버전 13.0.2, exact pin 아님 —
  `Microsoft.NET.Test.Sdk`가 전이적으로 13.0.3+를 요구해 exact pin은 NU1107
  충돌을 일으켰다). canonical 출력은 Newtonsoft 직렬화를 쓰지 않고
  `CanonicalJsonWriter`가 RFC 8785 JCS를 직접 구현한다.
- `dataVersion` 형식: `v1-` + canonical identity byte의 SHA-256 lowercase
  hex64 (`manifestHash`와 혼동 방지용 접두어).
- 테스트 전용 JSON Schema validator는 `JsonSchema.Net`(System.Text.Json 기반)
  사용 — Newtonsoft 버전 충돌을 피하기 위해 NJsonSchema 대신 선택.

**구현:**

- `schemas/` 4종(package-config, release-manifest, manifest-signature, channel,
  JSON Schema 2020-12) 작성. `release-manifest.schema.json`의 `parts` payload에
  누락됐던 `size`(전체 payload 크기, part 크기 합계 검증용) 필드를 추가로
  발견해 넣었다.
- `docs/contracts/gamepatchkit-yml.md` — YAML 입력 계약 문서, 07이 그대로 쓸
  `tests/fixtures/gamepatchkit-yml/{valid,invalid}/*.yml` 10종.
- `GamePatchKit.Core`: `Errors`(공통 오류 타입), `Paths`(경로 정규화·UTF-8 ordinal
  비교), `Globbing`(glob parser·matcher·`FileSelector` 선택 파이프라인),
  `Json`(`CanonicalJsonWriter`, `JsonNumbers` safe-integer), `Configuration`
  (`PackageConfig`+`Validator`), `Manifests`(`ReleaseManifest`+`ManifestValidator`+
  `ManifestIdentity`), `Signatures`(`ManifestSignature`), `Channels`(`Channel`).
  netstandard2.1 기본 LangVersion이 C# 8이라 file-scoped namespace 등 C# 9+
  문법은 쓰지 않았다(테스트 프로젝트는 net10.0이라 최신 문법 사용).
- golden vector 4종(`single-file`, `multipart-file`, `bundle-entry`, `mixed`
  — optional group·혼합 compression 포함)을 Python으로 독립 구현한 RFC 8785
  encoder로 생성하고 `shasum`으로 교차 검증한 뒤, 같은 모델을 C#으로 재현해
  byte-정확히 일치하는지 테스트한다(`tests/fixtures/golden-vectors/`).
- `tests/fixtures/{package-config,release-manifest,manifest-signature,channel}/
  {valid,invalid}/*.json` fixture와 이를 schema(JsonSchema.Net)·Core 모델 양쪽에
  검증하는 테스트.
- 경로·glob table-driven 테스트(`**` 0/1/여러 segment, NFC, 대소문자, exclude
  우선순위, 숨김 segment, group 중복, 입력 순서 무관 등).
- `GamePatchKit.Core.Tests` 107개 테스트 전부 통과(전체 solution 기준 115개).

**발견·수정한 버그:**

- `JsonSchema.Net`은 로드할 때마다 schema의 `$id`를 전역 registry에 등록한다.
  같은 schema 파일을 서로 다른 테스트 메서드에서 각각 `JsonSchema.FromFile`로
  불러오면 같은 `$id`가 두 번 등록되어 `$ref` 해석이 깨지고, **clean build 후
  첫 실행에서만 100% 재현되는** 실패가 났다(재실행하면 통과해 처음엔 flaky로
  오인했다). 파일당 스키마 인스턴스를 한 번만 로드해 캐싱하도록 고쳐 해결했다.
- 테스트 fixture 작성 중 파일 배열 정렬 순서를 실수로 틀리게 만든 것을
  `ManifestValidator`가 정확히 잡아냈다(`config-copy.json` vs `config.json`의
  ordinal 순서).
- 대화 도중 Directory.Packages.props·여러 csproj에 다른 세션이 NUKE 빌드/NuGet
  배포 자동화(`.nuke/`, `_build/`, `Directory.Build.props`, `build.sh`,
  `global.json`)를 동시 작업 중인 것을 발견했다. 사용자에게 확인 후 공유 파일은
  매번 다시 읽고 최소 범위로만 편집해 계속 진행했다.

**파일:** `schemas/*.schema.json`, `docs/contracts/gamepatchkit-yml.md`,
`src/GamePatchKit.Core/**`, `tests/GamePatchKit.Core.Tests/**`,
`tests/fixtures/**`, `Directory.Packages.props`,
`src/GamePatchKit.Core/GamePatchKit.Core.csproj`,
`tests/GamePatchKit.Core.Tests/GamePatchKit.Core.Tests.csproj`,
`docs/plan/02-schema-canonicalization.md`, `docs/plan/README.md`

### ✅ 수정/구현: Codex adversarial review 결과 8건 전부 수정

**배경:** 02 구현 직후 `codex:rescue`(adversarial 모드, model `gpt-5.6-terra`)로
`schemas/`, `src/GamePatchKit.Core/**`, `tests/GamePatchKit.Core.Tests/**` 범위를
review했다. 8건 모두 재현 가능한 실제 결함으로 확인해 전부 적용했다(reject 없음).

**적용한 수정:**

- **(High) unknown property가 `TryParse`를 실패시키지 않는 전 모델 공통 버그.**
  `errorList`에는 쌓이지만 별도로 추적하던 `ok` bool에는 반영되지 않아
  `additionalProperties:false`가 사실상 강제되지 않았다. `PackageConfig`,
  `PackageConfigGroup`, `ReleaseManifest`, `ManifestGroupEntry`, `FilePart`,
  `BundleEntry`, `FilePayload`, `FileSource`, `ManifestArtifact`,
  `ManifestFileEntry`, `ManifestSignature`, `Channel` 전부의 최종 성공 조건에
  `errorList.Count > 0` 검사를 추가했다. `PackageConfig`의 `groups`도 schema는
  required인데 Core는 optional로 처리하던 것을 맞췄다.
- **(High) `ManifestValidator`의 `files[].path` 중복 검사가 `StringComparer.Ordinal`만
  사용**해 대소문자만 다른 경로(OS 독립 충돌 규칙 위반)를 놓쳤다.
  `RelativePathNormalizer.FindCaseInsensitiveDuplicateGroups`를 추가로 적용했다.
- **(High) 하나의 file artifact를 공유하는 file들의 size·fileHash 일관성을
  검증하지 않았다.** 같은 artifactHash를 참조하는 첫 번째 file의 (size,
  fileHash)를 기록해 이후 참조와 다르면 거부하도록 추가했다.
- **(High) bundle entry 1:1 대응 검증이 불완전했다.** 같은 bundle 안에서
  `entries[].path`가 중복돼도(각각 1번씩만 참조되면) 잡지 못했고,
  `files[].path`가 `source.entryPath`와 달라도 통과했다. 둘 다 검사를 추가했다.
- **(Medium) multipart 크기 합계 계산이 `long` overflow에 취약했다.** part 2049개
  (2048개는 `2^53-1`, 1개는 2048 byte, 합계가 정확히 `2^64`)로 실제 재현
  테스트를 만들어, 미수정 상태에서 unchecked 덧셈이 0으로 wrap되어 조작된
  `parts.size: 0`과 우연히 일치해 통과했을 시나리오를 확인했다. 누적 중
  `MaxSafeInteger` 초과 시점에서 즉시 overflow로 표시하고 더 누적하지 않도록
  고쳤다.
- **(Medium) `GlobPattern`이 pattern 문자열을 NFC로 정규화하지 않았다.** NFD로 쓴
  pattern이 `RelativePathNormalizer`로 NFC 정규화된 후보 경로와 매칭에 실패할 수
  있었다. `TryParse`에서 NFC 정규화를 추가했다.
- **(Medium) `CanonicalJsonWriter`가 unpaired UTF-16 surrogate를 `Encoding.UTF8`의
  기본 replacement fallback으로 조용히 치환**해, unpaired surrogate 문자열과
  리터럴 U+FFFD 문자열이 같은 canonical byte(같은 hash)로 충돌할 수 있었다.
  throwing `UTF8Encoding`으로 바꿔 `FormatException`을 던지도록 고쳤다.
- **(Low) golden vector가 전부 ASCII라 JCS(UTF-16 code unit) key 정렬과 domain
  array의 UTF-8 byte 정렬이 실제로 다르다는 걸 증명하지 못했고, golden vector
  생성에 쓴 Python 스크립트도 세션 스크래치패드에만 있어 세션이 끝나면
  사라졌다.** U+1F600(surrogate pair) vs U+E000(단일 code unit)로 두 정렬이
  반대 순서를 만드는 것을 증명하는 테스트를 추가했고, Python 생성·검증
  스크립트를 `tests/fixtures/golden-vectors/tools/`에 커밋했다(`--check`
  옵션으로 커밋된 fixture와 재검증 가능).

모든 수정에 회귀 테스트를 추가했다. `GamePatchKit.Core.Tests`는 107 → 124개로
늘었고 clean rebuild 3회 반복 실행으로 안정성을 확인했다.

**파일:** `src/GamePatchKit.Core/Configuration/*.cs`,
`src/GamePatchKit.Core/Manifests/*.cs`, `src/GamePatchKit.Core/Signatures/*.cs`,
`src/GamePatchKit.Core/Channels/*.cs`, `src/GamePatchKit.Core/Globbing/GlobPattern.cs`,
`src/GamePatchKit.Core/Json/CanonicalJsonWriter.cs`,
`tests/GamePatchKit.Core.Tests/**`, `tests/fixtures/golden-vectors/tools/`,
`docs/plan/02-schema-canonicalization.md`

### 🔴 미완료 / 다음 창에서 이어갈 일

- 02는 여기서 완료(Codex adversarial review 8건 반영까지 포함). 다음은
  03(Core identity·manifest hash·diff·download plan) — 실제
  `dataVersion`/`manifestHash` 계산 API(streaming SHA-256 포함)는 03의
  책임이며, 02는 golden vector로 그 값들을 미리 고정만 해 뒀다.
- 07(CLI)이 `gamepatchkit.yml` YAML 파서 구현체와 그 동작 테스트를 만들 때
  `tests/fixtures/gamepatchkit-yml/`를 그대로 재사용한다.
- 11(서명)이 golden vector에 raw public key·`keyId`·`manifest.sig`를 추가할 때
  `tests/fixtures/golden-vectors/README.md`의 계약을 따른다.

### ✅ 수정/구현: 02단계 channel 구현 제거

**배경:** v1은 환경별 target 선택 모델·schema를 제공하지 않고 host 또는 서버가
target manifest를 직접 선택하는 것으로 책임 경계를 변경했다.

**수정:**

- `channel.schema.json`, Core `Channel`·`ChannelErrorCodes`, channel 전용
  단위 테스트와 schema fixture를 삭제했다.
- 공용 schema fixture 검증 대상을 `package-config`, `release-manifest`,
  `manifest-signature` 3종으로 줄였다.
- `KebabCaseId`와 `Hex64`의 channel 관련 주석을 현재 사용처에 맞게 정리했다.
- 활성 코드·schema·계획 문서에서 channel 참조가 남지 않은 것을 확인했다.
- `GamePatchKit.Core.Tests` 118개와 전체 solution 126개 테스트가 모두 통과했다.
- public API 삭제를 포함한 10개 파일 변경을 Codex adversarial self-review했으며
  추가 finding은 없었다.

**파일:** `schemas/channel.schema.json`, `src/GamePatchKit.Core/Channels/`,
`src/GamePatchKit.Core/{KebabCaseId,Hex64}.cs`,
`tests/GamePatchKit.Core.Tests/{Channels,SchemaFixtures}/`,
`tests/fixtures/channel/`

### ✅ 수정/구현: 03단계 Core identity·diff·download plan

**배경:** 02가 고정한 schema·canonical 계약 위에서 Packager와 Runtime이 공유할
순수 계산 로직(`docs/plan/03-core-identity-diff-plan.md`)을 구현했다. Core는
여전히 filesystem·HTTP·압축 구현을 참조하지 않는다.

**구현:**

- `Sha256Hash`: 64KiB 고정 버퍼 streaming SHA-256 → lowercase hex64. 1MiB 입력을
  한 번에 요구하지 않는 것을 `RecordingStream`으로 검증한다.
- `ReleaseIdentity`: canonical identity byte, `dataVersion`(`v1-` + hex64),
  canonical manifest byte, `manifestHash`, `Finalize(draft, compactVersion)`,
  그리고 byte ↔ 기대 `manifestHash` 검증 API. `Finalize`는 draft에 기록된
  `dataVersion`을 무시하고 다시 계산한다.
- `CompactVersionRule.Resolve`: candidate를 source `compactVersion`으로
  canonicalize해 byte가 같으면 `Changed=false`로 세 값을 그대로 재사용하고,
  다르면 `dataVersion` 유지 + `compactVersion + 1` + 새 `manifestHash`.
  candidate의 논리 상태가 source와 다르면 `ArgumentException`으로 거부한다.
- `ReleaseDiff`: 논리(`files[]` 기준 Added/Removed/ContentChanged/GroupMoved)와
  물리(저장 object 경로 기준 Added/Removed)를 분리해 각각 경로 순으로 정렬.
- `DownloadPlanner`: 명시적 target group 집합만 계획, 경로+검증된 `fileHash`가
  같으면 재다운로드 없음, bundle은 한 번만, part는 누락분만 + 결합 hash 검증
  요구, 예상 다운로드 byte·임시 공간·파일 수·bundle 수 계산.
- `ICompressionCodec`(async 전용)과 `CompressionCodecIds.Zstd`. Core assembly가
  압축 구현체를 참조하지 않는 것을 `TestAssemblyWiring`에서 확인한다.
- `ManifestArtifact.GetPayloadObjects()`를 추가해 diff·plan이 artifact 단위가
  아니라 실제 저장 object 단위로 동작하게 했다(같은 `artifactHash`의 재분할을
  잡기 위함).

**검증:** `GamePatchKit.Core.Tests` 163개(118 → 163), 전체 solution 171개 통과.
02의 golden vector 4종은 이제 `ReleaseIdentity`를 그대로 통과시켜 재현한다.

**파일:** `src/GamePatchKit.Core/{Sha256Hash,ICompressionCodec,CompressionCodecIds}.cs`,
`src/GamePatchKit.Core/Manifests/{ReleaseIdentity,FinalizedManifest,CompactVersionRule,CompactDecision,ArtifactPayloadObject,ManifestArtifact,ManifestErrorCodes}.cs`,
`src/GamePatchKit.Core/Diff/`, `src/GamePatchKit.Core/Downloads/`,
`tests/GamePatchKit.Core.Tests/{SampleManifests,TestSha256Hash,TestAssemblyWiring}.cs`,
`tests/GamePatchKit.Core.Tests/{Manifests,Diff,Downloads,GoldenVectors}/`

### 🔴 미완료 / 다음 창에서 이어갈 일

- 03 구현에 대한 Codex adversarial review는 아직 돌리지 않았다. 이 세션은
  Agent tool 사용이 막혀 있어 `/codex:adversarial-review --background`를
  사용자가 직접 실행해야 한다(신규 public API + 12개 신규 파일이라 트리거 대상).
  → 이후 실행했고 지적 3건을 모두 반영했다. 아래 "03단계 Codex adversarial
  review 3건 반영" 항목 참고.
- 다음 단계는 04(zstd codec adapter) — 03이 정의한 `ICompressionCodec`을
  `GamePatchKit.Compression.NativeCompressions`에서 구현한다.

### 🗒️ 메모

- Core 계산 API의 실패 표현 규칙을 03에서 확정했다: parser는 `bool` + error
  목록, validator는 `ValidationResult`, **계산 API는 선행 조건 위반 시 예외**.
  근거는 `docs/plan/03-core-identity-diff-plan.md`의 결정 사항 참고.

### ✅ 수정/구현: 03단계 Codex adversarial review 3건 반영

**배경:** 03 구현 직후 `/codex:adversarial-review`에서 `needs-attention` 3건.
핵심은 하나의 잘못된 가정이었다 — "저장 object는 경로로 유일하게 식별된다".

**원인:** part 경로는 `files/<artifactHash>/part-#####`로, 자기 byte가 아니라
**부모 artifact의 hash**로 주소화된다. 같은 30 byte payload를 20+10에서 16+14로
다시 나누면 `artifactHash`·part 개수·part 경로가 전부 그대로인 채 각 part의
크기와 `partHash`만 바뀐다.

**수정:**

- `DownloadPlanner`가 `IEnumerable<string>` 대신
  `IEnumerable<CachedArtifactObject>`(경로 + `objectHash`)를 받고 둘 다 일치할
  때만 cache hit으로 본다. 이전에는 옛 split의 part를 재사용해 결합 hash가 절대
  맞지 않는 계획을 만들 수 있었다.
- `ReleaseDiff`의 물리 비교 단위를 경로에서 `(경로, objectHash)`로 바꿨다. 같은
  경로에 다른 byte가 오면 Removed + Added 쌍으로 보고한다(정렬 키도 경로 →
  `objectHash`로 확장). 이전에는 재분할을 "물리 변경 없음"으로 봤다.
- `ReleaseIdentity.Finalize`가 draft의 collection을 복사한 뒤 identity를
  계산한다. `ReleaseManifest`가 collection을 참조로 보관하므로, 이전에는 호출자가
  원본 list를 나중에 수정하면 모델과 서명 대상 byte가 서로 다른 release를
  가리킬 수 있었다.
- part 경로 규칙 자체를 바꾸라는 권고는 반영하지 않았다. 02에서 고정한 PRD
  계약이고 schema·golden vector가 이미 그 값에 묶여 있다.

**검증:** Core 166개(163 → 166), 전체 solution 174개 통과. 세 수정을 임시로
되돌려 새 회귀 테스트 3개만 정확히 실패하는 것을 확인했다.

**파일:** `src/GamePatchKit.Core/Downloads/{CachedArtifactObject,DownloadPlanner}.cs`,
`src/GamePatchKit.Core/Diff/{ReleaseDiff,ArtifactChangeKind}.cs`,
`src/GamePatchKit.Core/Manifests/{ReleaseIdentity,ArtifactPayloadObject}.cs`,
`tests/GamePatchKit.Core.Tests/{Downloads,Diff,Manifests}/`

### ✅ 수정/구현: 03단계 Codex adversarial review 2차 2건 반영

**배경:** 1차 수정 후 재리뷰에서 `needs-attention` 2건. 둘 다 1차 수정이 **절반만
고쳤다**는 지적이었다.

**원인과 수정:**

- **재분할 충돌은 구분이 아니라 거부 대상이다.** 1차에서는 `(경로, objectHash)`로
  구분해 diff에 "같은 경로가 Removed + Added"로 표현했지만, 그건 메모리에서만
  구분한 것이다. 게시·전송·cache는 여전히 경로로 주소화하므로 두 세대는 실제로
  공존할 수 없다(PRD: 한 artifact 디렉터리에 payload 한 종류, 동일 경로 재업로드
  금지, hash 경로의 기존 byte 불일치는 오류). → `ReleaseStorageCompatibility`를
  추가해 두 release가 같은 경로에 다른 byte를 주장하면 `manifest.storage-path-conflict`
  로 보고하고, `ReleaseDiff.Compute`·`CompactVersionRule.Resolve`가 이를 gate로
  호출해 거부한다. gate 덕분에 diff 내부는 다시 경로 비교로 단순해졌다.
- **collection 복사가 중첩 list까지 내려가지 않았다.** 1차의 `Finalize` 스냅샷은
  바깥 list만 복사해서 `FilePayload.Parts.PartList`와
  `BundleArtifact.Entries`는 caller의 list를 그대로 aliasing했다. → 모델
  생성자에서 복사하도록 바꿨다(`ReadOnlySnapshot.Of`: `ReleaseManifest` 3개,
  `BundleArtifact.Entries`, `FilePayload.Parts.PartList`, `ManifestIdentity` 2개).
  이제 `Finalize`는 별도 스냅샷 없이도 불변이고, 모든 manifest가 생성 시점부터
  불변이다.
- part 경로 규칙에 partHash·세대를 넣으라는 권고는 이번에도 반영하지 않았다.
  02에서 고정한 PRD 계약이고 schema·golden vector가 그 값에 묶여 있다. 대신 위반
  manifest를 거부하는 쪽으로 같은 위험을 막는다.

**검증:** Core 172개(166 → 172), 전체 solution 180개 통과. 세 방어(중첩 복사,
diff gate, compact gate)를 임시로 되돌려 새 회귀 테스트 3개만 정확히 실패하는 것을
확인했다.

**파일:** `src/GamePatchKit.Core/Manifests/{ReleaseStorageCompatibility,ReadOnlySnapshot}.cs`(신규),
`src/GamePatchKit.Core/Manifests/{ReleaseManifest,ManifestArtifact,FilePayload,ManifestIdentity,ReleaseIdentity,CompactVersionRule,ArtifactPayloadObject,ManifestErrorCodes}.cs`,
`src/GamePatchKit.Core/Diff/{ReleaseDiff,ArtifactChangeKind}.cs`,
`src/GamePatchKit.Core/Downloads/CachedArtifactObject.cs`,
`tests/GamePatchKit.Core.Tests/{Manifests,Diff}/`

### ✅ 수정/구현: 03단계 Codex adversarial review 3차 1건 반영

**배경:** 2차에서 넣은 `ReleaseStorageCompatibility` gate가 **pairwise**라 우회
가능하다는 지적. 확인 결과 맞다.

**원인:** 저장소는 rollback을 위해 과거 release의 artifact를 계속 보관하는데,
gate는 직전 release 하나만 봤다. 3-release 순서로 뚫린다:

1. R1: payload H를 20+10 part로 저장 → `files/H/part-00000`, `part-00001` 점유
2. R2: 같은 H를 단일 object로 표현 → part 경로를 하나도 쓰지 않음
3. R3: H를 16+14로 재분할 → R2와 공유 경로가 없어 gate 통과, 그러나 아직 남아
   있는 R1의 part byte를 덮어씀

**수정:**

- `ReleaseStorageCompatibility.Validate(IEnumerable<ArtifactPayloadObject>
  retainedObjects, ReleaseManifest candidate)` 추가 — package가 보관 중인 전체
  object 목록 대 candidate. 기존 pairwise overload는 여기로 위임한다.
- inventory 자체가 한 경로에 서로 다른 object를 담고 있으면(이미 깨진 이력)
  그것도 보고한다. first-wins로 기록해, candidate가 둘 중 하나와 일치해도 모순이
  묻히지 않는다.
- `CompactVersionRule.Resolve`가 `retainedObjects`를 **필수 인자**로 받는다.
  생략 overload는 두지 않았다 — 없으면 "안 넘겨도 안전하다"는 오해가 그대로
  남는다. source 자신의 object는 항상 포함하므로 빈 목록은 "source가 저장소에
  있는 유일한 release"라는 뜻이다.
- `ReleaseDiff.Compute`는 pairwise 그대로 뒀다. diff는 "이 두 release가 공존
  가능한가"라는 질문 자체가 pairwise이고 게시 안전성을 주장하지 않는다. 다만
  주석에 그 경계를 명시했다.

**검증:** Core 175개(172 → 175), 전체 solution 183개 통과. inventory 인식과
self-conflict 보고를 임시로 되돌려 새 회귀 테스트 2개만 정확히 실패하는 것을
확인했다.

**파일:** `src/GamePatchKit.Core/Manifests/{ReleaseStorageCompatibility,CompactVersionRule}.cs`,
`src/GamePatchKit.Core/Diff/ReleaseDiff.cs`,
`tests/GamePatchKit.Core.Tests/Manifests/{TestReleaseStorageCompatibility,TestCompactVersionRule}.cs`

### 🗒️ 메모

- 05·06(Packager)에서 compact·publish를 구현할 때 `retainedObjects`를 실제
  저장소 목록으로 채워야 한다. 빈 목록을 넘기면 컴파일은 되지만 과거 release
  덮어쓰기를 못 잡는다.
- 아직 안 잡는 것 하나: 같은 `artifactHash`를 한 release는 single로, 다른
  release는 parts로 표현하는 경우. 경로가 겹치지 않아 byte 손상은 없지만 PRD
  "한 artifact 디렉터리에는 payload 한 종류만 존재"에는 어긋난다. artifactHash
  단위 payload 동일성 검사가 필요하며, 이번 지적 범위 밖이라 미뤘다.

### ✅ 수정/구현: 04단계 NativeCompressions zstd codec adapter

**배경:** 03에서 정의한 `ICompressionCodec`을 중앙 고정된
`NativeCompressions.Zstandard` 0.6.1로 구현하고, Runtime과 Packager가 구체
compression type 없이 주입받을 기본 codec factory가 필요했다.

**구현:**

- `ZstdCompressionCodecFactory.Create()`가 `ICompressionCodec`을 반환하고 실제
  codec class와 `NativeCompressions` type은 public API에 노출하지 않는다.
- 64KiB stream copy로 압축·해제하며 caller의 입출력 stream은 닫지 않는다.
- level 3, content size 비활성, content checksum 활성, dictionary ID 비활성,
  `NbWorkers=0`(caller-thread single-thread mode)으로 option을 고정했다.
- upstream option이 `init` setter를 사용하므로 adapter project만 C# 9를 명시하고
  `netstandard2.1` target은 유지했다.
- v1 계획을 수정하지 않고 `docs/plan/04-zstd-codec-adapter.md`와 plan README에
  `개발 v2` 구현 상태를 추가했다. 공개 호출 계약과 수동 smoke test 절차는
  `docs/contracts/zstd-codec.md`에 기록했다.

**Adversarial review 반영:** upstream `ZstandardStream`은 0-byte 입력에서 내부
buffer가 생성되지 않으면 dispose 때 frame footer를 쓰지 않는다. 압축 stream에
empty write를 먼저 수행해 encoder를 초기화하고 빈 파일 round-trip 회귀 테스트를
추가했다.

**검증:** codec test 7개와 전체 solution 189개가 모두 통과했다. 현재 macOS arm64
smoke test는 통과했으며, Windows·Linux와 다른 architecture는 해당 환경에서
`Category=PlatformSmoke`를 수동 실행해야 한다.

**파일:** `src/GamePatchKit.Compression.NativeCompressions/`,
`tests/GamePatchKit.Compression.NativeCompressions.Tests/`,
`docs/contracts/zstd-codec.md`, `docs/plan/{04-zstd-codec-adapter,README}.md`

### ✅ 수정/구현: zstd malformed·truncated frame 거부

**증상:** `NativeCompressions.Zstandard` 0.6.1의 `ZstandardStream` 해제 경로는
checksum 불일치나 잘못된 frame에서 `InvalidData`를 소비 없이 반복해 hang하고,
frame footer가 잘린 입력은 `NeedMoreData` 상태에서 EOF를 성공으로 반환했다.

**수정:**

- 해제 구현을 `ZstandardStream.CopyToAsync`에서 low-level
  `ZstandardDecoder` loop로 교체했다.
- pool 기반 64KiB 입력·출력 buffer로 streaming을 유지하면서 `InvalidData`,
  decoder 무진행과 frame 완료 전 EOF를 `InvalidDataException`으로 거부한다.
- cancellation은 각 상태 전이와 stream read/write에 전달하고, 성공·실패·취소
  모든 경로에서 pooled buffer를 반환한다.
- 잘못된 magic, checksum 1-byte 변조, 빈 압축 stream, 정상 frame의 마지막
  1~4 byte 절단을 회귀 테스트로 고정했다. 2MiB round-trip은 압축 입력을 13-byte
  chunk로 읽혀 `NeedMoreData`와 `DestinationTooSmall` 전이를 함께 검증한다.
- codec 사용 문서의 예외·부분 출력 폐기 계약과 04 계획의 `개발 v2` 완료 상태를
  갱신했다.

**검증:** 수정 전 truncated test 4개가 모두 “예외 없음”으로 실패했고, 수정 후
codec test 14개와 전체 solution 196개가 모두 통과했다. Codex adversarial
self-review에서 추가 finding은 없었다.

**파일:** `src/GamePatchKit.Compression.NativeCompressions/ZstdCompressionCodecFactory.cs`,
`tests/GamePatchKit.Compression.NativeCompressions.Tests/TestZstdCompressionCodec.cs`,
`docs/contracts/zstd-codec.md`, `docs/plan/04-zstd-codec-adapter.md`

### ✅ 수정/구현: 05단계 Packager file artifact·최초/incremental package

**배경:** 03의 manifest·identity 계약과 04의 zstd adapter를 실제 filesystem
source와 불변 publish tree에 연결하는 Packager pipeline이 비어 있었다.

**구현:**

- `FilePackageBuilder.BuildAsync`가 validated `PackageConfig`, output root와 선택적인
  이전 canonical manifest를 받아 최초·incremental release를 생성한다.
- Windows volume/file ID와 Unix device/inode를 stable identity로 사용하고,
  symlink·junction·reparse point를 따르지 않는 source snapshot, 열린 handle
  identity·크기·수정 시각의 hash 전후 비교와 final 재열거를 구현했다.
- Core glob 선별 결과를 UTF-8 byte ordinal로 유지하고, output이 source 안에 있거나
  output 경로에 link가 있으면 게시 전에 거부한다.
- 무압축·zstd payload, `maxArtifactBytes` 고정 part, content-addressed staging,
  기존 object byte 검증·재사용과 immutable path 충돌 거부를 구현했다.
- config·manifest schema를 embedded resource로 포함해 JSON Schema → Core 의미·참조
  → 실제 payload 순서로 검증한다. payload 검증은 object·part SHA-256, 결합
  `artifactHash`, zstd 해제 후 원본 크기·`fileHash`를 streaming으로 확인한다.
- incremental은 file artifact를 group·mode·compression 변경과 무관하게 재사용하고,
  유효한 기존 bundle 전체를 유지한다. bundle entry 일부가 달라지면 미참조 entry
  금지 계약 때문에 해당 bundle의 현재 파일 전체를 file override로 전환한다.
- canonical `manifest.json`, 선택적 `manifest.json.zst`, typed
  `PackageBuildReport`를 제공한다. 비결정적 report는 publish tree에 기록하지 않는다.
- v1 플랜 본문은 보존하고 05와 plan README의 `개발 v2` 완료 상태, 공개 사용 문서를
  추가했다.

**Adversarial review 반영:**

- part별 hash와 원본 복원만으로는 부모 `artifactHash` 불일치를 놓치므로 ordered part
  결합 payload의 크기와 SHA-256을 별도로 검증한다.
- lexical output 경로 안의 기존 symlink가 root 밖을 가리킬 수 있어 output root와
  존재하는 하위 path component의 reparse/link를 거부한다.

**검증:** Packager test 25개 통과. same-input 결정성, default file mode, single·parts,
zstd·compressed manifest, compression-only no-op, 삭제·group 이동, bundle 전체
재사용·전체 fallback, 손상 object·part·부모 hash, immutable collision, source
추가·삭제·교체·내용 변경과 source/output symlink를 포함한다.

**파일:** `src/GamePatchKit.Packager/`,
`tests/GamePatchKit.Packager.Tests/TestFilePackageBuilder.cs`,
`docs/contracts/file-packager.md`, `docs/plan/{05-packager-file-artifacts,README}.md`

### ✅ 수정/구현: 06단계 deterministic bundle·compact

**배경:** 최초 package의 bundle group 생성과 incremental file override를 실제
bundle로 다시 통합하는 compact pipeline이 비어 있었다. compact는 논리 파일 상태와
`dataVersion`을 유지하면서 물리 배치가 달라질 때만 `compactVersion`을 올려야 했다.

**구현:**

- 실행별 값을 포함하지 않는 직접 구현 POSIX PAX writer로 entry 순서, header 이름,
  `path`·`size`·`mtime` record, UID/GID, mode, mtime과 정확히 두 tar end block을
  byte 단위로 고정했다.
- 최초 bundle group을 `maxArtifactBytes`에 맞춰 entry 경계로 분할하고, 단일 entry가
  실제 payload 기준으로도 제한을 넘을 때 file artifact/part로 fallback한다.
- `BundleCompactor`가 source manifest의 bundle·file artifact에서 선택 group을
  staging에 복원하고 현재 bundle 정책으로 재생성한다. 비선택 group은 그대로
  유지하며 전체 retained object inventory로 과거 release 경로 충돌을 검사한다.
- candidate의 canonical byte가 같으면 게시 없는 성공 no-op으로 기존 identity를
  반환하고, 물리 배치가 바뀐 경우에만 `compactVersion`을 증가시켜 새 manifest를
  게시한다.
- `PackagePayloadVerifier`가 bundle SHA-256뿐 아니라 archive를 canonical PAX로 다시
  직렬화해 숨겨진 extended-header byte, metadata, entry 내용과 종단까지 검증한다.
  zstd 해제 출력은 manifest에서 계산한 정확한 tar 크기로 제한한다.
- v1 계획 본문은 보존하고 06과 plan README의 `개발 v2` 완료 상태, 공개 사용 계약을
  추가했다. 기존 검증된 file artifact 재사용은 현재 `maxArtifactBytes`보다 크더라도
  불변 표현과 compression metadata를 유지하도록 경계를 명시했다.

**Adversarial review 반영:**

- framework `TarWriter`의 PAX extended-header 이름이 process마다 달라지는 문제를
  golden hash가 고정된 직접 writer로 교체했다.
- `TarReader`가 숨기는 non-canonical PAX header 이름을 놓치지 않도록 raw tar와
  canonical 재직렬화 결과의 길이·SHA-256을 비교한다.
- 변경 compact에서 `compactVersion`이 JSON safe integer 상한을 넘을 때 내부
  `NotSupportedException`을 노출하지 않고 `packager.manifest-invalid`로 거부한다.
- 비정상 codec이 작은 zstd object를 과도하게 확장해도 예상 tar 크기에서 즉시
  `InvalidDataException`으로 중단한다.

**검증:** Packager test 45개와 전체 solution 240개가 통과했다. 별도 fixture 간
동일 PAX byte와 고정 SHA-256, UTF-8 장경로, group 격리, bundle 분할·file part
fallback, zstd round-trip, 손상 payload·metadata·PAX header·추가 end block,
compact no-op·override 통합·선택 group 재압축·retained object 충돌·버전 상한을
포함한다.

**파일:** `src/GamePatchKit.Packager/`,
`tests/GamePatchKit.Packager.Tests/{PackageFixture,TestBundlePackageBuilder,TestBundleCompactor}.cs`,
`docs/contracts/{bundle-compact,file-packager}.md`,
`docs/plan/{06-bundle-compact,README}.md`

### ✅ 수정/구현: 08단계 Runtime 상태 머신

**배경:** Core download plan 뒤의 platform 독립 실행 계층이 비어 있어 target
manifest 검증, cache·staging, required/optional group 전환과 package 단위 atomic
activation을 adapter마다 다시 구현해야 했다.

**구현:**

- `PackageRuntime`이 host가 지정한 `packageId`·`dataVersion`·`manifestHash`를
  검증하고 최초·전역 설치는 required group만, optional API는 현재 active
  manifest의 요청 group만 계획하도록 구현했다.
- `IArtifactTransport`와 `IRuntimeStorage`에 manifest/artifact stream, package writer
  lock, atomic state byte 교체, content-addressed cache, exact installation file-set,
  group staging·immutable promotion과 seekable scratch stream contract를 정의했다.
- package별 `PackageState` v1 canonical JSON 모델과 serializer/validator를 추가했다.
  `ready`·`notInstalled`·`stale`, 전체 group 집합·정렬, active manifest hash,
  opaque non-absolute `installationKey`와 I-JSON revision 상한을 검증한다.
- cache object 크기·SHA-256 재검증, transient retry, multipart part 결합
  `artifactHash`, artifact별 무압축/zstd 해제, 최종 file 크기·`fileHash`와
  deterministic PAX bundle byte 계약 검증을 streaming으로 구현했다.
- 한 요청의 모든 group installation을 승격한 뒤 writer lock 안에서 시작 snapshot의
  revision과 active identity를 재확인하고 state 전체를 한 번 교체한다. 충돌은 최신
  state로 재계획하고 취소·실패에서는 이전 state와 검증 cache를 유지한다.
- unchanged optional과 compact의 동일 파일은 기존 exact installation을 새 manifest에
  재연결하고, changed optional은 `stale`, optional→required는 active 교체 전에
  `ready`로 만든다. 손상 state는 trusted target이 있는 전역 설치에서만 재구성한다.
- 공개 사용·adapter 수명 계약을 `docs/contracts/runtime.md`에 작성하고 v1 계획 본문을
  보존한 `08-runtime.md` 개발 v2와 plan README 완료 상태를 갱신했다.

**Adversarial review 반영:**

- 이미 설치된 selected group도 manifest가 선언한 codec 지원 여부를 확인한다.
- 실제 state 변화가 없는 no-op은 revision safe-integer 상한에서도 새 revision 없이
  성공하고, 변경이 필요한 경우만 상한 오류로 거부한다.
- multipart part stream을 동시에 열지 않고 scratch에 순서대로 결합해 file handle
  수가 part 개수에 비례하지 않게 했다.
- target hash만 같은 기존 installation에 삭제 파일이 남을 수 있어 adapter file path
  전체를 target과 exact-set으로 비교하고, 다른 경우 target 파일만 새 installation에
  staging한다.
- PAX·압축·복원 실패는 `runtime.artifact-corrupted`, writer lock·atomic replace
  실패는 `runtime.activation-failed`로 정규화했다.

**검증:** Runtime test 29개와 전체 solution 353개가 Release 구성에서 통과했다.
required-only·optional batch, partial state 부재, 취소 후 cache 재사용, retry,
revision 충돌·상한, state/installation 손상 복구, optional 전환, file 삭제,
single·multipart·canonical PAX bundle, 실제 NativeCompressions zstd 혼합 group,
canonical·semantic manifest와 payload 손상을 포함한다. Runtime production/test
format과 `git diff --check`도 통과했다.

**파일:** `src/GamePatchKit.Runtime/`,
`tests/GamePatchKit.Runtime.Tests/`,
`docs/contracts/runtime.md`,
`docs/plan/{08-runtime,README}.md`

### ✅ 수정/구현: 07 CLI `gpk`와 Packager verify·sign API

**배경:** 05·06으로 package·compact가 완성돼 CI에서 호출할 진입점이 필요했고, PRD가
verify·sign 규칙을 CLI가 아니라 Packager에 두고 다른 host와 공유하도록 요구한다.

**수정:**

- Packager에 CLI와 독립적인 `ReleaseVerifier`를 추가했다. schema → Core 의미·참조
  무결성 → 실제 payload byte → signature document 순서로 검증하고 typed report를
  반환한다. 문서 검증만 필요한 diff·download plan·retained inventory는
  `ReleaseManifestReader`를 쓴다.
- `PreviousReleaseReader`가 하던 manifest 문서 검증을 `ManifestDocumentReader`로
  뽑아 incremental package·verify·sign·retained inventory가 같은 순서를 공유한다.
- `ReleaseSigner`가 canonical manifest와 `manifestHash`를 검증한 뒤 주입된
  `IManifestSigner`로 `manifest.sig` 모델과 canonical byte를 만든다. 같은 byte는
  검증 후 재사용하고 다른 key ID·signature는 불변 경로 충돌로 실패한다.
- `FilePackageRequest`·`BundleCompactRequest`에 `DryRun`을 추가했다. 계산은 그대로
  하고 게시만 생략하므로 identity는 실제 실행과 같고 output tree는 바뀌지 않는다.
- `LocalInstallationScanner`가 설치 tree와 object cache를 실제 hash로 스캔하고,
  `RetainedReleaseInventory`가 보관 중인 release의 object 경로를 모은다.
- CLI에 `package`·`diff`·`verify`·`compact`·`plan-download`·`sign`을 구현했다.
  exit code는 `0`/`1`(입력)/`2`(무결성)/`3`(실행 실패)이고 `--json`은 canonical JSON
  envelope 한 줄을 출력한다.
- `YamlConfigDocument`가 YamlDotNet event stream을 직접 읽어 단일 non-empty
  document·mapping root만 허용하고 anchor·alias·merge key·custom tag·중복 key를
  규칙별 코드로 거부한다.

**확정 결정:**

- Ed25519는 `BouncyCastle.Cryptography`로 고정했다. .NET 10에는 Ed25519가 없고,
  netstandard2.0 target 순수 관리 코드라 07의 서명(net10.0)과 11의 Core
  검증(netstandard2.1)이 같은 구현을 공유할 수 있다.
- YAML은 `YamlDotNet`을 쓰되 object model이 아니라 low-level event parser를 쓴다.
  deserializer는 anchor·merge key를 해석한 뒤 결과만 주고 중복 key를 덮어쓰므로
  금지 규칙을 강제할 수 있는 지점이 event stream뿐이다.
- YAML plain scalar는 null·boolean·I-JSON 안전 범위 10진 정수만 typed 값으로 읽는다.
  나머지는 문자열이 되어 schema type 오류로 거부되므로 canonical JSON이 표현할 수 없는
  값이 설정에서 만들어지지 않는다.
- 이전 release·compact source·retained release는 모두 `manifestHash`로만 지정한다.
  `--retained`는 자동 수집하지 않는다. 디렉터리 목록은 보관 중인 release의 object와
  잔여물을 구분할 수 없다.
- `verify`는 caller가 준 `manifestHash`와 비교하고 재계산하지 않는다. 재계산하면
  자기 일관적인 모든 manifest가 통과한다.

**검증:** Packager 76개, CLI 54개를 포함해 전체 solution 348개가 통과했다. 02가 고정한
`gamepatchkit-yml` fixture 10건의 규칙별 수락·거부(검증 기준 24), 손상된 part·bundle·
manifest를 원본과 같은 길이로 덮어쓴 뒤의 exit code `2`(검증 기준 11), compact no-op의
`changed: false`와 무변경 output tree(검증 기준 9), sign의 key ID 파생·재사용·불변 충돌·
개인키 비노출(검증 기준 25 서명 생성 범위), 그리고 같은 fixture를 Packager API와 CLI로
실행한 결과 일치를 포함한다. 서명 byte는 BouncyCastle Ed25519 verify로 독립 검증한다.

**🔴 다음 창에서 이어갈 일:**

- 11단계에서 `IManifestSignatureVerifier`를 Core 검증 API로 구현해 연결하면
  `verify`의 `signature.state`가 `verified`가 된다. 지금은 `present`까지만 보고한다.
- `compact --retained`는 운영자가 빠뜨리면 조용히 보호가 약해진다. 운영 스크립트에서
  보관 release 목록을 만드는 방법을 13단계 문서에 남겨야 한다.

**파일:** `src/GamePatchKit.Cli/`, `src/GamePatchKit.Packager/`,
`tests/GamePatchKit.Cli.Tests/`, `tests/GamePatchKit.Packager.Tests/`,
`docs/contracts/{cli,gamepatchkit-yml,file-packager,bundle-compact}.md`,
`docs/plan/{07-cli,README}.md`, `Directory.Packages.props`

### ✅ 수정/구현: 07 adversarial review 반영

**배경:** 07 CLI 작업 후 adversarial review에서 4건이 나왔고, 3건이 내 코드에서 재현됐다.

**수정:**

- `--require-signature`가 위조 서명을 통과시켰다. verifier가 없으면 `ReleaseVerifier`는
  형식만 맞는 `keyId`와 아무 64 byte짜리 `manifest.sig`를 `present`로 보고하고 exit `0`을
  낸다. 실제로 all-zero public key에서 파생한 `keyId`와 `0xff` 64 byte로 재현했다.
  `ReleaseVerifyRequest.RequireSignature`는 `SignatureVerifier` 없이 지정하면 거부하고,
  CLI는 07에서 서명 필수 option을 아예 제공하지 않는다. `signature.state`가 무엇을
  확인했고 무엇을 확인하지 않았는지 그대로 보고한다.
- `Math.Abs(long.MinValue)`가 던지는 `OverflowException`이 `Program`에서 잡히지 않아
  `schemaVersion: -9223372036854775808`짜리 설정이 exit 134와 stack trace로 죽었다.
  경계를 상·하한 비교로 바꾸고, 최상위에 마지막 `catch (Exception)`을 두어 모든 종료가
  문서화된 네 exit code 중 하나에 들어가게 했다.
- `ReadScalar`가 `Scalar.Tag`를 보지 않아 `!!str 1`이 정수 1이 됐다. 내장 tag를
  허용한다고 해놓고 무시하고 있었다. 명시적 tag가 철자·인용보다 우선하도록 고치고,
  허용 tag를 값 모델이 정의한 `!!str`·`!!int`·`!!bool`·`!!null`·`!!map`·`!!seq`로
  한정했다. `!!float`·`!!binary`는 내장 tag여도 canonical JSON이 표현할 수 없어 거부한다.

**Runtime manifest 읽기 상한 (08 단계 파일, 사용자 지시로 함께 처리):**

- `PackageRuntime.ReadManifestBytesAsync`가 transport stream을 상한 없는
  `MemoryStream`에 복사한 뒤에야 `manifestHash`를 검증했다. 고장났거나 악의적인
  endpoint가 첫 무결성 검사 전에 client 메모리를 고갈시킬 수 있다.
- 64 MiB 상한을 넘는 순간 더 읽지 않고 `runtime.manifest-invalid`로 중단한다. 재시도하지
  않는다. 이 manifest일 수 없는 크기의 응답은 일시적 실패가 아니라 고장난 endpoint이고,
  재시도는 상한을 두 번 더 소진할 뿐이다.
- artifact object는 이미 `RuntimeHashingWriteStream`이 manifest가 선언한 크기를 상한으로
  streaming 검증하므로 상한이 필요 없다. 상한이 없는 응답은 manifest 하나뿐이었다.

**자체 발견:** `BuiltInTag_IsAllowedWhileAnApplicationTagIsNot` 테스트가
`(string?)document["packageId"]`로 단언하고 있었다. 정수 token도 `(string?)` 캐스팅이
`"1"`을 주므로 값이 틀린 채로 통과했다. token type 단언으로 바꿨다.

**검증:** 전체 solution 376개 통과. 네 수정을 각각 임시로 되돌려 확인했다 —
`RequireSignature` 사전조건 1건, `Math.Abs` 경계 2건, tag 해석 5건, manifest 상한 2건만
실패하고 나머지는 통과했다. manifest 상한을 되돌리면 끝나지 않는 stream을 읽다
`MemoryStream`이 2GiB 한계에서 `IOException: Stream was too long`으로 죽는다. 실제 CLI
바이너리로 위조 서명·`long.MinValue`·`!!str` 재현 케이스가 모두 exit `1`과 JSON envelope로
바뀐 것도 확인했다.

**파일:** `src/GamePatchKit.Cli/{Program,CliErrorCodes}.cs`,
`src/GamePatchKit.Cli/{Configuration/YamlConfigDocument,Commands/VerifyCommand}.cs`,
`src/GamePatchKit.Packager/{ReleaseVerifyRequest,SignatureState}.cs`,
`src/GamePatchKit.Runtime/PackageRuntime.cs`,
`tests/GamePatchKit.Cli.Tests/`, `tests/GamePatchKit.Packager.Tests/TestReleaseVerifier.cs`,
`tests/GamePatchKit.Runtime.Tests/TestManifestReadBound.cs`,
`docs/contracts/{cli,gamepatchkit-yml,runtime}.md`, `docs/plan/07-cli.md`

### ✅ 수정/구현: 07 adversarial review 2회차 — Runtime 복구 전이

**배경:** manifest 읽기 상한을 넣은 뒤 다시 돌린 adversarial review에서 2건이 나왔다. 둘 다
`PackageRuntime`의 복구 전이이고, 07 CLI·Packager 코드에는 지적이 없었다. 08 단계 파일이지만
사용자 지시로 여기서 처리했다.

**수정:**

- `LoadStateAsync`가 현재 active manifest를 읽을 때 `ManifestInvalid`만 복구 대상으로 삼았다.
  과거 release의 manifest를 회수하면 `TransportFailed`가 그대로 빠져나가, 새 target과 artifact가
  멀쩡한데도 client가 갱신할 수 없고 로컬 state를 손으로 지워야만 풀렸다. `allowRecovery`일 때는
  이유를 가리지 않고 검증된 target에서 다시 만든다. 호출자가 이미 target을 검증했고 재사용하는
  installation은 전부 target 기준으로 다시 검증하므로 안전하다.
- `TransitionOptionalGroupAsync`가 재사용 실패 시 항상 `stale`을 유지했다. A→B로 stale이 된
  optional 데이터가 손상된 뒤 A로 rollback하면 `verifiedManifestHash`가 target과 같은 `stale`이
  되는데, `PackageStateValidator`는 이를 거부한다. 매 rollback이 `state-invalid`로 실패하고 빠져
  나갈 방법이 없었다. 마지막 검증 manifest가 곧 target이면 `notInstalled`로 전이한다.

**트레이드오프:** 첫 수정은 일시적 장애로 옛 manifest를 못 읽어도 state를 버린다. optional group을
다시 설치해야 하지만, "영구히 갱신 불가"보다는 낫다는 판단이다. 더 보수적인 대안(manifest 없이도
group 목록만 살리기)은 `StateSnapshot` 의미를 바꾸는 08 설계 변경이라 하지 않았다.

**검증:** 전체 solution 379개 통과. 두 수정을 함께 되돌리면 새 테스트 2건만 실패한다. 온전한
stale 데이터의 rollback이 여전히 다운로드 없이 `ready`로 재연결되는지도 함께 확인해, 새 guard가
정상 경로의 재사용을 뺏지 않는지 검증했다.

**파일:** `src/GamePatchKit.Runtime/PackageRuntime.cs`,
`tests/GamePatchKit.Runtime.Tests/{TestRuntimeStateRecovery.cs,FakeRuntimeAdapters.cs}`,
`docs/contracts/runtime.md`

### ✅ 수정/구현: 07 adversarial review 3회차 — 선언 크기 미강제 3건

**배경:** 2회차에서 Runtime manifest에만 상한을 넣었더니, 3회차 리뷰가 "같은 논리가 적용돼야
하는데 안 된 곳"을 전부 찾아왔다. 2회차에 내가 "manifest가 유일한 무제한 읽기"라고 판단한 건
Runtime 안에서만 맞았고 Packager는 보지 않았다.

**수정:**

- file artifact 압축 해제에 상한이 없었다. 저장 object는 크기·SHA-256이 고정이지만 **해제 결과
  크기는 고정이 아니다.** 작고 자기 일관적인 zstd object가 무한정 팽창할 수 있고, 크기 검사는
  해제가 끝난 뒤에 돌았다. `verify`에서는 CPU를, **`compact` 복원에서는 실제 파일에 직접 쓰므로
  디스크를** 소모한다(리뷰가 언급하지 않은 경로). `MaximumLengthWriteStream`을
  `BundleArchiveReader` 내부 중첩 클래스에서 공용 타입으로 올려 두 곳에 걸었다. bundle 해제는
  처음부터 이 상한을 쓰고 있었다.
- `ReleaseManifestReader.ReadPublishedBytesAsync`와 `manifest.sig` 읽기가 `File.ReadAllBytesAsync`로
  전체를 할당한 뒤에야 검증했다. `BoundedFile`로 manifest 64 MiB(Runtime과 동일), 서명 4 KiB
  상한을 걸었다. 길이를 먼저 보고, FIFO·문자 장치처럼 길이를 못 믿는 경우를 위해 읽는 중에도 센다.
- `StreamMatchesAsync`가 EOF까지 다 읽은 뒤 크기를 비교했다. 선언 크기까지만 읽고 한 byte를 더
  확인하는 방식으로 바꿨다. 모든 state 로드와 download plan이 지나는 경로다.

**설계 메모:** 사용자가 분할 압축으로 해결하는 안을 제시했으나 채택하지 않았다. 분할해도 chunk별
선언 크기를 강제하지 않으면 chunk 하나가 여전히 무한 팽창하고, 강제하는 순간 단일 frame에서도
똑같이 막힌다. 게다가 frame 경계 변경은 `artifactHash` 정의를 바꿔 golden vector를 무효화한다.
분할 압축은 임의 접근·병렬 해제 근거로 12단계 측정 뒤 판단할 사안이다(PRD 1033행).

**검증:** 전체 solution 386개 통과. 세 상한을 함께 되돌리면 Packager 2건이 실패하고 Runtime
cache 테스트는 **끝나지 않아 watchdog에 SIGKILL(exit 137)** 된다 — 상한이 없으면 끝없는 cache
stream을 영원히 읽는다는 뜻이다. 압축 폭탄 테스트는 codec이 실제로 만들어낸 byte 수를 세어
`선언 크기 + 청크` 이내에서 멈췄는지 확인한다. 정직한 codec과 선언 크기에 정확히 맞는 cache
항목이 여전히 통과하는지도 함께 확인해, 상한이 정상 경로를 깨지 않는지 검증했다.

**파일:** `src/GamePatchKit.Packager/{MaximumLengthWriteStream,BoundedFile,PackageLayout}.cs`,
`src/GamePatchKit.Packager/{PackagePayloadVerifier,BundleArchiveReader,BundleCompactor}.cs`,
`src/GamePatchKit.Packager/{ReleaseManifestReader,ReleaseVerifier,ReleaseSigner}.cs`,
`src/GamePatchKit.Runtime/PackageRuntime.cs`,
`tests/GamePatchKit.Packager.Tests/TestDecodeBounds.cs`,
`tests/GamePatchKit.Runtime.Tests/{TestCacheStreamBound,FakeRuntimeAdapters}.cs`,
`docs/contracts/{file-packager,runtime}.md`

### ✅ 수정/구현: 09 DotNet adapter (`GamePatchKit.DotNet`)

**배경:** 08단계 Runtime의 `IArtifactTransport`/`IRuntimeStorage`를 일반 .NET 환경(HTTP
전송, filesystem 저장소)에 연결하는 구체 구현이 없었다.

**구현:**

- `HttpArtifactTransport` — 외부 주입 `HttpClient` 기반. manifest·signature 경로는
  Packager의 `PackageLayout`과 같은 문자열을 이 프로젝트가 직접 만든다(서로 참조 불가 관계라
  공유 못 함). artifact 경로는 Runtime이 주는 `relativePath`를 그대로 쓴다 — 이미
  `packageId/artifacts/...` 접두사를 포함하므로 다시 붙이지 않는다. HTTP 상태코드를
  408/429/5xx → transient, 나머지 실패 상태 → permanent로 분류하고, 재시도는 전혀 하지
  않는다(Runtime의 몫). `HttpClient.Timeout`이 호출자 token 취소 없이
  `OperationCanceledException`을 던지는 함정을 `cancellationToken.IsCancellationRequested`로
  구분해 진짜 취소만 감싸지 않고 통과시켰다.
- `FileSystemRuntimeStorage` — `packages/<packageId>/{state,cache,installs,staging}`.
  state 교체는 임시 파일 + `Flush(flushToDisk: true)` + `File.Move(..., overwrite: true)`
  원자적 rename. writer lock은 lock 파일 **존재 여부**가 아니라 `FileShare.None` 독점 handle
  획득 여부로 판단(요구사항 원문이 명시적으로 금지한 방식). `installationKey`는
  `"packageId/localId"` opaque 문자열로 만든다 — `InstallationExistsAsync` 등 세 멤버가
  `packageId`를 받지 않아 key 자체에 실어야 했다. `AdapterPath`는 Packager의
  `PackagePath.Resolve`와 같은 root-escape·symlink 방어를 별도로 유지한다(참조 불가 관계라
  공유 못 함 — 향후 Core로 옮겨 공유할 여지는 있으나 이번 범위에서는 하지 않았다).
- PRD가 나열한 다섯 디렉터리 중 `manifests/`는 만들지 않았다. `IRuntimeStorage`의 어떤
  멤버도 storage에 manifest byte caching을 요청하지 않기 때문이다(Runtime은 항상
  `IArtifactTransport`로 다시 읽는다). 문서에 이유를 남기고 빈 디렉터리·죽은 코드를 만들지
  않았다.
- 시작 시 필수 package 검증(ASP.NET·worker 기동 실패 옵션)은 별도 wrapper 클래스를 만들지
  않았다 — 기존 `InstallOrUpdateAsync`를 시작 경로에서 그대로 부르고 예외를 전파시키는 것과
  완전히 같아서, 감싸는 순간 아무 동작도 없는 순수 위임이 되기 때문이다. 대신 계약 문서에
  호출 예시를 남겼다.
- `DefaultCompressionCodecs.Create()`로 기본 zstd codec 주입을 한 줄로 만들었다.

**테스트:** unit 30개(state 원자성·writer lock 교차 인스턴스 차단·cache commit/복구·staging
promote/abandon·scratch stream, HTTP transient/permanent 분류·취소 구분) +
`GamePatchKit.IntegrationTests`에 실제 filesystem + fake HTTP handler + 실제
`FilePackageBuilder` 출력을 엮은 시나리오 19개(최초 설치·optional 후속·unchanged 재연결·
변경분 stale 전환·요청 순서 무관성·batch 부분 실패 시 미활성화·state 손상/미지원 schema
복구(신뢰 target 유무)·동시 writer 차단·손상 응답이 cache를 오염시키지 않음·취소 후 재개
cache 재사용·state 교체 실패 시 이전 state 유지·zstd 압축 포함 Packager fixture
end-to-end). HTTP fake 서버는 실제 요청 순서를 기록해 두 파일 중 어느 쪽이 먼저 받아지는지
(content hash 순서라 파일 경로 순서와 무관) 추측 대신 관찰해서 취소 테스트를 만들었다.

**파일:** `src/GamePatchKit.DotNet/*.cs`(신규),
`tests/GamePatchKit.DotNet.Tests/*.cs`(신규),
`tests/GamePatchKit.IntegrationTests/{AdapterHarness,FakePublishServer,PublishedPackageFixture,ManifestPayloadLookup,FailingReplaceStorageDecorator,TestDotNetAdapter*}.cs`(신규),
`tests/GamePatchKit.IntegrationTests/GamePatchKit.IntegrationTests.csproj`,
`docs/contracts/dotnet-adapter.md`(신규), `docs/plan/09-dotnet-adapter.md`

### ✅ 수정/구현: 09 DotNet adapter adversarial review 1회차 — 6건 적용

**배경:** `codex-collab-workflow` adversarial review(`gpt-5.6-terra`)가 High 3·Medium 4·Low 1을
보고했다.

**적용:**

- `HttpResponseBodyStream.Read`/`ReadAsync`가 header 단계 실패만 감싸고 body 도중
  `HttpRequestException`은 그대로 흘려보냈다. 대용량 artifact 전송 중 흔한 연결 끊김이
  Runtime의 재시도를 완전히 건너뛰던 결함이다. body read도 같은 규칙으로 감쌌다.
- `SplitInstallationKey`가 첫 `/`의 존재만 검사해 `"pkg//tmp"` 같은 key가
  `localId == "/tmp"`로 쪼개지고 `Path.Combine`이 이를 Unix에서 절대 경로로 취급해
  `installs/` 밖으로 탈출할 수 있었다. `packageId`/`localId` 양쪽을 각각
  `KebabCaseId.IsValid`/`Guid.TryParseExact`로 재검증했다 — 손상됐지만 schema상 유효한
  state 파일이 이 경로로 들어올 수 있는 입력이다.
- writer lock 획득이 모든 `IOException`을 "다른 writer가 잠그고 있다"로 간주해
  `CancellationToken.None`이면 무한 대기했다. disk full 같은 무관한 오류도 똑같이
  보였다. 30초 예산을 두고 넘기면 마지막 예외를 담아 던진다.
- cache·installation·state reader가 `FileShare.Read`만 써서 Windows에서
  `File.Move(overwrite:true)`가 sharing violation으로 실패할 수 있었다(Unix는 무관).
  `FileShare.Read | FileShare.Delete`로 바꿨다 — 이 macOS sandbox에서는 재현이 안 되는
  플랫폼 특정 결함이라 fix만 적용하고 red/green 테스트는 못 만들었다.
- 동시 writer 통합 테스트가 lock을 no-op으로 바꿔도 통과했다. 같은 target을 향하는 두
  writer는 Runtime 자체의 낙관적 동시성 재시도(state 읽기→비교→교체)만으로도 같은 결과에
  수렴하기 때문이다. `AcquirePackageWriterLockAsync`가 잡혀 있는 구간을 직접 계측하는
  decorator로 바꿔, no-op lock으로 되돌리면 5/5 재현으로 확실히 실패하는 것을 확인했다.
- 취소 후 재개 테스트가 100ms 취소·500ms 지연의 타이밍 경쟁이었다. 요청을 무기한 잡아 둘
  수 있는 gate로 바꾸고, 첫 artifact가 실제로 cache에 commit된 걸 확인한 뒤에만 취소하도록
  했다.

**보류(Defer):** `AdapterPath.Resolve`의 symlink 검사는 check-then-use 구조라 TOCTOU
여지가 있다. Packager의 `PackagePath.Resolve`와 완전히 같은 구조라 새로 생긴 약점이
아니라 두 구현이 이미 공유하는 것이며, 제대로 고치려면 `openat`+`O_NOFOLLOW` 계열의
플랫폼별 저수준 구현이 필요해 이번 단계 범위를 넘어선다. 신뢰 전제를 문서에 명시하는
선에서 남겨 뒀다 — 사용자가 더 깊은 수정을 원하는지 판단할 사안이다.

**검증:** 전체 solution 435개 통과, 경고 0건(기존 `_build` NU1903 제외). 위 6건 중
platform-specific 1건을 뺀 나머지는 모두 fix를 되돌리면 실패하는 걸 직접 확인했다.

**파일:** `src/GamePatchKit.DotNet/{HttpResponseBodyStream,AdapterLayout,FileSystemRuntimeStorage}.cs`,
`src/GamePatchKit.DotNet/GamePatchKit.DotNet.csproj`,
`tests/GamePatchKit.DotNet.Tests/{TestHttpArtifactTransport,TestFileSystemRuntimeStorageState,TestFileSystemRuntimeStorageInstallation}.cs`,
`tests/GamePatchKit.IntegrationTests/{AdapterHarness,FakePublishServer,ExclusivityTrackingStorageDecorator,TestDotNetAdapterRecovery,TestDotNetAdapterTransportFailures}.cs`(일부 신규),
`docs/contracts/dotnet-adapter.md`

### ✅ 수정/구현: 09 DotNet adapter adversarial review 2회차 — 4건 적용

**배경:** 사용자가 보류 항목(`AdapterPath` TOCTOU)을 제외하고 같은 Codex thread를 이어서
2회차를 요청했다. 1회차에서 고친 6건 중 2건이 자기 자신도 완전히 고치지 못했다는 걸
2회차가 찾아냈다 — 매 회차가 같은 문제를 한 겹씩 더 파고드는 이 세션의 반복 패턴이 이번
step에서도 그대로 나타났다.

**적용:**

- `HttpArtifactTransport.GetAsync`가 header 수신 이후 `response.Content.ReadAsStreamAsync`
  단계의 실패는 감싸지 않았다. 1회차에서 고친 건 `HttpResponseBodyStream`(stream이 이미
  만들어진 뒤)의 body read뿐이라, header와 body read 사이의 이 지점은 여전히 raw
  `HttpRequestException`을 내보냈다. 같은 규칙으로 감싸는 catch 절을 추가했다.
- `HttpResponseBodyStream`의 동기 `Read`는 `HttpRequestException`만 감싸고
  `OperationCanceledException`은 감싸지 않았다 — 주석과 계약 문서는 "Read와 ReadAsync가
  같은 정책"이라고 주장했지만 실제로는 아니었다. 동기 `Read`는 token 자체가 없으므로
  받는 어떤 취소 모양 예외든 timeout으로 간주해 감쌌다.
- 동시 writer 통합 테스트가 여전히 **하나의** `FileSystemRuntimeStorage` instance를 두
  `PackageRuntime`이 공유했다. instance-local(in-memory) lock으로 바꿔도 이 시험은
  통과했을 것이다 — 실제로 확인해보니 `Expected: 1, Actual: 2`로 정확히 잡아냈다. 두
  개의 **독립된** storage instance로 나누고, `SharedLockObserver`로 카운터만 공유하도록
  다시 짰다. 또한 `Task.WhenAll`의 스케줄링 운에 기대는 대신, 첫 writer를 lock이 잡힌
  구간 안에서 명시적으로 붙잡아 두고 그동안 두 번째가 실제 OS lock에서 막히는 것까지
  결정론적으로 확인하게 했다.
- 취소 후 재개 테스트가 첫 artifact가 cache에 commit된 것만 기다리고 취소했다. 두 번째
  요청이 실제로 서버에 도착해 hold gate에 걸린 상태인지는 확인하지 않아, 취소가 의도한
  지점이 아닌 다른 곳에서 발생해도 시험이 통과할 수 있었다. 두 번째 요청의 open count가
  1 이상이 될 때까지 추가로 기다린 뒤 취소하도록 했다.

**검증:** 전체 solution 437개 통과, 경고 0건. 4건 모두 fix를 되돌리면 실패하는 걸
확인했다. 특히 동시 writer 테스트는 no-op lock과 instance-local lock 두 가지 깨진
구현 모두에서 각각 5/5 확실히 실패하는 것까지 확인했다.

**파일:** `src/GamePatchKit.DotNet/{HttpArtifactTransport,HttpResponseBodyStream}.cs`,
`tests/GamePatchKit.DotNet.Tests/TestHttpArtifactTransport.cs`,
`tests/GamePatchKit.IntegrationTests/{SharedLockObserver,ExclusivityTrackingStorageDecorator,TestDotNetAdapterRecovery,TestDotNetAdapterTransportFailures}.cs`(일부 신규)

### ✅ 09 DotNet adapter adversarial review 3회차 — clean

**배경:** 사용자가 같은 thread로 3회차(보류 항목 제외)를 요청했다.

**결과:** Codex가 "deferred `AdapterPath.Resolve` TOCTOU 항목을 제외하면 추가
Apply-worthy finding은 없다"고 명시적으로 결론지었다. 2회차 fix 4건과 그 위에 쌓인
1회차 fix들을 다시 읽고 대조했으나 torn-read, response leak, lock 동시 보유, promotion
cleanup 회귀를 찾지 못했다. Codex 쪽 sandbox가 MSBuild 임시 디렉터리 생성을 막아 자체
테스트 실행은 못 했다고 밝혔다 — 이 결론은 정적/호출 경로 검토 기준이며, 실행 검증은
이 세션에서 직접 돌린 437개 테스트로 이미 확보돼 있다.

**리뷰 루프 종료:** 3/3회차 완료, 마지막 회차가 clean이라 순리대로 종료. 남은 항목은
사용자가 두 회차 연속 명시적으로 제외한 `AdapterPath` TOCTOU 하나뿐이다(보류 유지).

### ✅ 수정/구현: 10단계 adapter conformance fixture·test suite

**배경:** 09단계까지 완료(별도로 커밋됨 확인)한 뒤 사용자가 10단계 진행을 요청했다.
배포 형태(NuGet test package vs 소스 fixture)는 계획 문서에 열린 결정 사항으로
남아있어 사용자에게 먼저 확인했다 — 소스 fixture로 지금 구현하고 NuGet 패키징은
13단계로 미루기로 했다.

**구현:**

- 새 프로젝트 `tests/GamePatchKit.Conformance/` — `ConformanceFixture`(Packager로
  실제 release 생성, `Group(name, required, mode)`에 `ArtifactMode` 추가),
  `InMemoryArtifactTransport`/`InMemoryRuntimeStorage`(SemaphoreSlim 기반 실제
  writer lock을 가진 reference adapter, installationKey는 GUID 하나로만 구성해
  DotNet의 `"packageId/localId"` 형식과 의도적으로 다르게 함), 계측용 decorator
  4종(`Counting`/`Corrupting`/`HoldingArtifactTransportDecorator`,
  `FailingGroupPromotionStorageDecorator`) — 모두 인터페이스만 감싸는
  adapter-무관 wrapper라 두 adapter에 동일하게 적용된다.
- `ConformanceTestBase`(abstract xUnit test base, 17개 시나리오): required-only
  설치, optional group notInstalled/ready/stale 전환(재다운로드 없는 재연결
  포함), 취소 후 재개, 손상 artifact 거부, state 교체 실패 주입, multi-group
  batch 단일 group 실패 시 부분 ready 미노출, 서로 다른 optional group에 대한
  동시 설치가 revision 충돌을 감지하고 유실 없이 재시도, writer lock 배타성,
  bundle+zstd, multipart file, 그리고 `tests/fixtures/release-manifest/invalid/*.json`
  5종을 재사용한 unsigned negative manifest 거부(theory).
- 09단계에서 만든 `FailingReplaceStorageDecorator`·`ExclusivityTrackingStorageDecorator`
  ·`SharedLockObserver`·`ManifestPayloadLookup`을 `GamePatchKit.IntegrationTests`에서
  `GamePatchKit.Conformance`로 옮기고 `internal`→`public`으로 바꿔 두 프로젝트가
  공유한다(중복 코드 방지). `TestConformanceInMemoryAdapter`(Conformance)와
  `TestConformanceDotNetAdapter`(IntegrationTests)가 같은 base를 상속해 실제로
  같은 17개 시나리오를 두 adapter에서 돌린다.

**버그 발견 및 수정:** `WriterLock_TwoConcurrentCallers_NeverHeldSimultaneously`가
in-memory adapter에서 `MaxObservedConcurrentLocks == 2`로 실패했다. 원인은
`ExclusivityTrackingStorageDecorator.TrackedLock.DisposeAsync()`가 실제 lock을
먼저 해제하고 observer count를 나중에 감소시키던 순서에 있었다 — `SemaphoreSlim`처럼
빠른 lock은 두 번째 holder가 즉시 깨어나 `EnterAsync()`를 호출하는 게 첫 holder의
`Exit()` 호출보다 먼저 일어날 수 있다. 이 09단계 코드가 그동안 문제를 드러내지 않은
건 DotNet adapter의 lock이 25ms 폴링 방식이라 이 경합 창을 우연히 가려왔기 때문이다.
순서를 뒤집어(`Exit()` 먼저, 그다음 실제 lock 해제) 고쳤다 — 되돌리면 5/5 확실히
실패하고 고치면 5/5 통과하는 것을 확인했다.

**검증:** 전체 solution 471개 테스트 통과(기존 454 + 신규 Conformance 17). 같은 17개
시나리오가 in-memory adapter 단독 실행과 DotNet adapter(IntegrationTests) 실행 모두에서
동일하게 통과한다.

**파일:** `tests/GamePatchKit.Conformance/**`(신규), `tests/GamePatchKit.IntegrationTests/TestConformanceDotNetAdapter.cs`(신규),
`docs/contracts/adapter-conformance.md`(신규), `docs/plan/10-adapter-conformance.md`,
`docs/plan/README.md`

### ✅ 수정/구현: 10단계 adversarial review 1회차 — 7건 적용

**배경:** 10단계 구현 완료 뒤 새 Codex thread로 adversarial review를 요청했다(09단계
리뷰와 무관한 신규 코드라 사용자가 새 thread를 선택). 결과: `High` 3건, `Medium`
3건, `Low` 1건.

**적용:**

- `UnsignedInvalidManifestFixture_IsRejected`의 fixture 5개 중 4개는 raw byte가
  pretty-print JSON이라 canonical byte 불일치만으로 거부돼, `ManifestValidator`가
  실제 결함(중복·참조·정렬)을 잡았는지는 증명하지 못했다. 별도 스크립트로
  `ReleaseManifest.TryParse`·`ManifestValidator.Validate`·raw-vs-canonical byte를
  직접 확인해 Codex의 지적이 정확함을 검증했다. 파싱 성공한 fixture는
  `ReleaseIdentity.ComputeCanonicalBytes`로 재직렬화해 전달하고 `ManifestValidator`가
  실제로 invalid를 반환하는지 사전 assert를 추가했다.
- 같은 theory가 `tests/fixtures/release-manifest/invalid/*.json`을 프로젝트 경로로
  직접 읽어, "소스 fixture로 배포" 시 monorepo 구조가 없으면 fixture를 못 찾고
  실패했다. `GamePatchKit.Conformance.csproj`에 `EmbeddedResource`로 내장하고
  `Assembly.GetManifestResourceNames()`로 읽도록 바꿨다 — 이제 이 프로젝트만
  떼어가도 동작한다.
- `WriterLock_TwoConcurrentCallers_NeverHeldSimultaneously`가 `Task.Delay(200)`으로
  두 번째 caller가 lock 획득을 "시도했다"고 가정했다 — 실제로는 시도 여부를
  확인하지 않아, 두 번째가 아직 도달하지 않았을 뿐인데도 no-op lock이 통과할 수
  있었다. `SharedLockObserver`에 `RecordAttempt()`/`AttemptCount`를 추가해 실제
  시도 신호를 기다리도록 바꿨다.
- `MultiGroupBatch_OneGroupPromotionFails_*`에 실패가 의도한 group("beta")에서
  발생했는지 확인하는 `exception.Error.Group` assertion을 추가했다.
- `InMemoryRuntimeStorage.SemaphoreLockHandle.DisposeAsync()`가 idempotent하지
  않아 같은 handle을 두 번 dispose하면 permit이 두 번 반환될 수 있었다(reference
  adapter로서 좋지 않은 예시). `Interlocked.Exchange` 기반 disposed flag로 고치고,
  전용 테스트(`TestInMemoryRuntimeStorage.cs`)를 새로 추가했다 — 되돌리면
  `SemaphoreFullException`으로 즉시 실패, 고치면 통과.
- `FailingReplaceStorageDecorator`/`FailingGroupPromotionStorageDecorator`의
  `_remainingFailures` 감소가 동기화되지 않아 동시 호출 시 카운트가 깨질 수
  있었다. `Interlocked.CompareExchange` CAS loop로 바꿨다.
- `StateReplaceFailureInjection_*`이 실제로는 adapter 내부 쓰기 도중 atomicity가
  아니라 "replace 호출 자체가 실패했을 때 Runtime이 이전 state를 유지하는지"만
  증명한다는 점을 코드 주석과 contract 문서에 명시했다 — 진짜 adapter-internal
  atomicity는 generic decorator로 재현할 수 없어 DotNet adapter 전용 테스트가
  별도로 담당한다.

**거부:** in-memory adapter가 `CreateStorage()`에서 같은 instance를 반환하는 것,
`MultiGroupBatch_*`가 batch 진행 "도중" partial state를 관찰하지 않는다는 지적은
의도된 설계로 판단해 거부했다 — 전자는 단일 프로세스 store에서 "다른 object,
같은 backing"이 애초에 성립하지 않고, 후자는 `PackageRuntime`이 batch당 정확히
한 번만 `ReplacePackageStateAsync`를 호출하는 걸 소스에서 재확인해 중간 state가
adapter 버그 없이는 애초에 쓰일 수 없다. 두 판단 근거를 contract 문서의 "알려진
범위 제한" 절에 남겼다.

**검증:** 전체 solution 472개 테스트 통과(17→18, idempotent-dispose 전용 테스트
추가). fixture 재직렬화는 별도 스크립트로 4개 fixture 모두 `TryParse=True,
semanticValid=False, rawEqualsCanonical=False`임을 확인. idempotent dispose fix는
되돌리면 즉시 실패·복원하면 통과를 확인.

**파일:** `tests/GamePatchKit.Conformance/{ConformanceTestBase,InMemoryRuntimeStorage,
SharedLockObserver,ExclusivityTrackingStorageDecorator,FailingReplaceStorageDecorator,
FailingGroupPromotionStorageDecorator,GamePatchKit.Conformance.csproj}.cs`(일부 신규
`TestInMemoryRuntimeStorage.cs`), `docs/contracts/adapter-conformance.md`

### ✅ 수정/구현: 11단계 서명·key rotation

**배경:** 07이 `ManifestSignature` 모델·`Ed25519ManifestSigner`·`ReleaseSigner`/
`ReleaseVerifier`·CLI `sign`을 이미 구현하며 `IManifestSignatureVerifier` 확장점을
11 전용으로 남겨뒀다. 11의 실제 작업은 그 확장점에 진짜 암호 검증을 채우고
Runtime 신뢰 모델을 추가하는 것이었다.

**구현:**

- `GamePatchKit.Core.Signatures.Ed25519Signatures`(신규, Core): `DeriveKeyId`·
  `Verify`. BouncyCastle.Cryptography가 netstandard2.0 대상이라 07의 결정대로
  netstandard2.1 Core에서도 같은 구현을 그대로 썼다 — 검증 전용 별도 구현은
  필요 없었다. `Ed25519ManifestSigner.DeriveKeyId`(Packager)는 이 함수로
  위임하도록 바꿔 중복 로직을 제거했다. `ManifestSignature.GetSignatureBytes()`도
  추가해 base64url 디코드를 한 곳에 모았다.
- `TrustedKeySignatureVerifier`(신규, Packager)와 CLI `verify`의
  `--trusted-key`(반복 가능)/`--require-signature`. `--trusted-key` 없이
  `--require-signature`만 주면 기존 `ReleaseVerifyRequest` 검증이 입력 오류로
  거부한다(변경 없음, 07이 이미 만든 gate).
- `TrustedSigningKeys`(신규, Runtime)와 `PackageRuntime` 생성자의
  `trustedSigningKeys`·`requireSignature` 선택 인자. 없으면(기본값)
  `OpenManifestSignatureAsync`를 아예 호출하지 않아 08 동작과 완전히 같다.
  있으면 `LoadManifestAsync` 마지막에 서명을 검증하며, 있는데 미신뢰 key거나
  검증 실패면 `requireSignature`와 무관하게 항상 거부하고 누락은
  `requireSignature`일 때만 거부한다. `RuntimeErrorCodes.SignatureInvalid`(신규)
  하나로 세 실패를 모두 보고한다.
- signature를 못 가져오는 것(미등록·재시도 소진)과 "없음"을 같은 것으로
  취급하기로 하면서, `FakeArtifactTransport`(Runtime.Tests)·
  `InMemoryArtifactTransport`(Conformance)가 미등록 manifestHash에 빈 stream을
  돌려주던 08~10의 placeholder 동작을 실제 not-found 예외로 고쳤다 — 지금까지
  아무도 이 메서드를 실제로 쓰지 않아 드러나지 않았던 자리다.
- golden vector에 `public-key.bin`/`key-id.txt`/`manifest-sig.canonical.json`
  추가(고정 32-byte 테스트 key, `SigningKeys.PrivateKey()`와 동일). Python
  `cryptography`(OpenSSL Ed25519 - BouncyCastle과 다른 구현)로 서명해 `jcs.py`가
  canonicalization에 대해 하는 역할을 signing에 대해서도 하게 했다.
- RFC 8032 Section 7.1 vector 5개(TEST 1/2/3/1024/SHA(abc))를 rfc-editor.org
  원문에서 스크립트로 파싱해 옮겨 손 transcription 실수를 없앴다. bit-flip
  (message·public key·signature R/S)·잘못된 길이·non-canonical signature(S + 곡선
  order L) negative test 추가 — non-canonical 거부는 BouncyCastle이 이미
  하고 있다는 걸 실행해서 확인했을 뿐 Core에 별도 검사를 추가하지는 않았다.
- adapter conformance suite(10단계)에 signed 시나리오 4개 추가: 신뢰 key
  검증 성공, `requireSignature: true`에서 누락 거부, 미신뢰 key 서명 거부
  (`requireSignature` 무관), bit-flip 손상 서명 거부. 새 abstract 메서드
  `RegisterSignatureAsync`가 두 adapter 각각의 위치에 signed fixture를 둔다.

**검증:** 전체 solution `dotnet test` 519개 테스트 통과(Core 195·Runtime 43·
Compression 14·Conformance 22·Packager 91·DotNet 37·Cli 77·IntegrationTests 40).
non-canonical signature 거부는 실제로 S+L 값을 만들어 `Verify`가 `false`를
반환하는지 실행해 확인했다(가정이 아니라 실행 결과).

**파일:** `src/GamePatchKit.Core/Signatures/{Ed25519Signatures.cs,ManifestSignature.cs}`,
`src/GamePatchKit.Packager/{Ed25519ManifestSigner.cs,TrustedKeySignatureVerifier.cs,
PackageErrorCodes.cs}`, `src/GamePatchKit.Cli/{Commands/VerifyCommand.cs,ExitCode.cs}`,
`src/GamePatchKit.Runtime/{TrustedSigningKeys.cs,PackageRuntime.cs,RuntimeErrorCodes.cs}`,
golden-vectors 4개 디렉터리 + `tools/generate_golden_vectors.py`,
`tests/GamePatchKit.Core.Tests/Signatures/TestEd25519Signatures.cs`(신규),
`tests/GamePatchKit.Conformance/{ConformanceTestBase,InMemoryArtifactTransport,
TestConformanceInMemoryAdapter}.cs`, `tests/GamePatchKit.IntegrationTests/
TestConformanceDotNetAdapter.cs`, `tests/GamePatchKit.Runtime.Tests/
{FakeRuntimeAdapters,TestPackageRuntime,SigningKeys}.cs`(SigningKeys 신규),
`tests/GamePatchKit.Packager.Tests/TestTrustedKeySignatureVerifier.cs`(신규),
`tests/GamePatchKit.Cli.Tests/TestCliCommands.cs`,
`docs/contracts/{runtime,cli,adapter-conformance}.md`,
`docs/plan/{11-signing-key-rotation.md,README.md}`

### ✅ 수정/구현: 11단계 adversarial review 1회차 — 2건 적용, 1건 문서화하고 보류

**적용:**

- `TrustedKeySignatureVerifier.FromBase64UrlPublicKeys`가 `encodedPublicKeys`에
  `null` 원소가 있으면 `PackageErrorCodes.InvalidTrustedKey`로 감싸지 않고
  `NullReferenceException`을 그대로 흘렸다. `encoded == null` 검사를 조건에
  추가했다. 되돌리면 `NullReferenceException`, 고치면
  `PackageException(InvalidTrustedKey)`을 확인했다.
- Runtime 서명 검증 테스트 공백 4건을 채웠다: signature fetch의 transient
  재시도 후 성공(재시도 횟수까지 확인), 4 KiB 초과 응답 거부,
  `InstallOptionalGroupsAsync`가 이미 active인 manifest의 서명도 다시 검증한다는
  것(설치 후 서명을 tamper하고 optional group 설치를 시도해 거부되는지 확인),
  key rotation 전체 시나리오(구·신 key 동시 신뢰 중엔 구 key release가 설치되고,
  구 key를 신뢰 목록에서 뺀 뒤에는 그 release가 거부되며 신 key로 서명한 새
  release는 설치된다).

**보류(문서화):** signature를 가져오는 transport 호출이 "확인된 not-found"와
"이번엔 이유 불문 실패"를 구분하지 못해, `requireSignature: false`에서는
signature 응답을 지속적으로 실패시킬 수 있는 공격자·오동작 장비가 실제
서명된 release를 unsigned로 위장시킬 수 있다는 지적. `manifestHash`가 내용
자체는 이미 보장하므로 내용 변조로는 이어지지 않지만, "서명이 숨겨졌다"와
"애초에 없었다"를 구분 못 하는 것은 사실이다. `requireSignature: true`에는
이 틈이 없다(못 가져오면 항상 거부). 제대로 고치려면 `ArtifactTransportException`에
"확인된 not-found" 신호를 추가하는, 이 diff보다 범위가 넓은 `IArtifactTransport`
계약 변경이 필요해 지금은 미루고 `docs/contracts/runtime.md`에 "알려진 한계"로
명시했다. 현재(의도된) 동작은
`Signature_TransientFailuresExhausted_IsTreatedAsMissingNotAsTransportFailure`
테스트로 고정해뒀다.

**검증:** 전체 solution `dotnet test` 통과(Runtime 43→48, Packager 91→92).

**파일:** `src/GamePatchKit.Packager/TrustedKeySignatureVerifier.cs`,
`tests/GamePatchKit.Packager.Tests/TestTrustedKeySignatureVerifier.cs`,
`tests/GamePatchKit.Runtime.Tests/{FakeRuntimeAdapters,TestPackageRuntime}.cs`,
`docs/contracts/runtime.md`

### ✅ 수정/구현: 11단계 adversarial review 2회차 — 3건 적용

**배경:** 2회차 첫 시도(task-ms1ps605-atlk7u)가 진행 중 응답 없이 죽어서(로그
12분간 정지, 추적 PID 소멸 — 이전에도 있었던 환경 이슈) 그 job을 취소하고 같은
thread에서 재시도해 완료했다.

**적용:**

- 1회차에서 문서화하고 보류했던 항목을 2회차가 더 정밀하게 재지적했다: signature
  fetch가 transient 실패로 재시도를 다 소진해도 `requireSignature`와 무관하게
  "서명 없음"과 동일하게 취급되고 있었다. `ArtifactTransportException.IsTransient`가
  이미 "이건 404가 아니다"를 보장하므로 이 경우는 계약 변경 없이도 구분 가능한데
  구분하지 않고 있었다는 것이 핵심 지적이다. `ReadManifestSignatureBytesAsync`를
  고쳐 transient 소진은 `runtime.transport-failed`로 항상 하드 실패하도록
  바꿨다. 되돌리면(마지막 catch 제거) 관련 테스트가 "예외가 발생하지 않음"으로
  실패, 복원하면 통과를 확인했다. 진짜 구분 불가 지점(non-transient 실패가
  404인지 401/403인지)은 여전히 `IArtifactTransport` 계약 변경이 필요해
  documented·deferred 상태를 유지한다.
- `TrustedKeySignatureVerifier.Verify`가 `IManifestSignatureVerifier` 계약
  ("거부는 `false`, throw는 신뢰 key 집합 자체가 못 쓸 때만")을 어기고, 신뢰하는
  keyId를 가졌지만 signature 문자열이 깨진 `ManifestSignature`(`TryParse`를
  거치지 않고 직접 생성된 경우)에 대해 `InvalidOperationException`을 흘렸다.
  `ManifestSignature`에 non-throwing `TryGetSignatureBytes`를 추가하고
  `TrustedKeySignatureVerifier`·Runtime 서명 검증 양쪽 모두 이를 쓰도록 바꿨다.
  되돌리면 `InvalidOperationException`, 고치면 `false` 반환을 확인했다.
- 새로 추가했던 테스트 2개가 실제로는 주장을 증명하지 못했다: retry-exhaustion
  테스트는 `SignatureOpenCount`를 확인하지 않아 첫 실패에서 바로 포기해도
  통과했을 것이고, oversized 테스트는 5,000-byte 배열이 4 KiB 검사 없이도 JSON
  파싱 실패로 같은 에러 코드를 냈을 것이다. 각각 `SignatureOpenCount == 3`
  assertion과 에러 메시지의 "byte limit" 문자열 assertion을 추가해 실제로
  주장하는 경로를 관측하도록 고쳤다.

**검증:** 전체 solution `dotnet test` 526개 통과(Packager 93). 두 코드 fix
모두 되돌려서 테스트가 실패하는 것을 확인한 뒤 복원했다.

**파일:** `src/GamePatchKit.Runtime/PackageRuntime.cs`,
`src/GamePatchKit.Core/Signatures/ManifestSignature.cs`,
`src/GamePatchKit.Packager/TrustedKeySignatureVerifier.cs`,
`tests/GamePatchKit.Runtime.Tests/TestPackageRuntime.cs`,
`tests/GamePatchKit.Packager.Tests/TestTrustedKeySignatureVerifier.cs`,
`docs/contracts/runtime.md`, `docs/plan/11-signing-key-rotation.md`

### ✅ 수정/구현: `IArtifactTransport`에 확인된 not-found 신호 추가 (2회차 review 보류 항목 해결)

**배경:** 2회차 review에서 보류했던 마지막 항목 — signature fetch 실패 중
"진짜 not-found"(예: HTTP 404)와 "확인 안 된 non-transient 실패"(예: 401/403)를
구분 못 하는 문제. 사용자가 직접 고쳐달라고 요청해 plan mode로 설계를 세우고
승인받은 뒤 구현했다.

**구현:**

- `ArtifactTransportException`에 `bool IsNotFound`를 세 번째 optional 생성자
  매개변수(기본값 `false`)로 추가했다. 아직 외부 배포 어셈블리가 없는 v1
  단계라 기존 생성자 시그니처를 그대로 보존하는 별도 오버로드까지는 두지 않았다.
- `HttpArtifactTransport.GetAsync`가 HTTP 404일 때만 `isNotFound: true`를 넘긴다.
  401/403/410을 포함한 다른 client error는 `false` 그대로.
- `InMemoryArtifactTransport`(Conformance)·`FakeArtifactTransport`(Runtime.Tests)의
  "등록 안 됨" throw를 전부 `isNotFound: true`로 통일해, 두 adapter가 실제
  transport의 계약을 일관되게 흉내내도록 했다.
- `PackageRuntime.ReadManifestSignatureBytesAsync`의 catch 구조를 정리했다:
  `IsNotFound`인 경우만 "서명 없음"으로 tolerate하고, 그 외(transient 소진이든
  확인 안 된 non-transient 실패든)는 전부 `runtime.transport-failed`로 하드
  실패한다 — 이게 이번에 닫은 틈이다.
- 새 테스트: `Signature_NonTransientFailureNotConfirmedAbsent_
  IsAHardFailureEvenWithoutRequireSignature`(Runtime.Tests, 닫은 틈을 직접 증명),
  `OpenArtifactAsync_NotFound_IsConfirmedAbsence`/
  `OpenArtifactAsync_OtherPermanentClientErrors_AreNotConfirmedAbsence`
  (DotNet.Tests, 404만 `IsNotFound: true`임을 확인).

**검증:** 전체 solution `dotnet test` 통과(Runtime.Tests 49, DotNet.Tests 41,
Conformance·IntegrationTests는 회귀 없이 기존 개수 유지 — 두 adapter 모두
"서명 진짜 없음" 시나리오가 여전히 tolerate됨을 확인).

**파일:** `src/GamePatchKit.Runtime/{ArtifactTransportException,PackageRuntime}.cs`,
`src/GamePatchKit.DotNet/HttpArtifactTransport.cs`,
`tests/GamePatchKit.Conformance/InMemoryArtifactTransport.cs`,
`tests/GamePatchKit.Runtime.Tests/{FakeRuntimeAdapters,TestPackageRuntime}.cs`,
`tests/GamePatchKit.DotNet.Tests/TestHttpArtifactTransport.cs`,
`docs/contracts/runtime.md`, `docs/plan/11-signing-key-rotation.md`

### ✅ 수정/구현: `/codex:review` 지적 — non-canonical base64url signature 인코딩 수용

**배경:** `/codex:review`(네이티브 리뷰어)가 새로 짚은 항목: `ManifestSignature`의
base64url 디코드가 padding 안 쓰이는 bit이 0이 아니어도 통과시켜, 같은 64 byte를
가리키는 서로 다른 signature 문자열이 둘 다 "유효"하게 된다는 지적. 코드 주석은
"Convert.FromBase64String가 non-zero padding bit를 거부한다"고 적혀 있었는데,
직접 실행해서 확인한 결과 이 주장 자체가 틀렸다(`"AA=="`와 `"AP=="` 둘 다 byte
0으로 디코드됨) — 07단계부터 있던, 한 번도 실행 검증 안 된 가정이었다.

**구현:**

- `ManifestSignature.TryDecodeBase64Url`(Core)을 고쳐 디코드한 byte를 다시
  인코딩해 원본 문자열과 byte 단위로 비교한다 — 일치하지 않으면 거부. 이 저장소가
  이미 canonical JSON byte에 쓰는 것과 같은 "재직렬화 후 비교" 패턴이다.
- 같은 조사 중 `Base64Url.TryDecode`(Packager, `--trusted-key`·개인키 파싱에 사용)도
  똑같이 틀린 주석과 로직을 갖고 있는 걸 발견해 같은 방식으로 고쳤다 — signature의
  "manifest.sig 불변 byte" 계약처럼 실제로 깨지는 계약은 없었지만(공개/개인키는
  raw byte만 쓰이고 문자열 자체를 비교하는 곳이 없음), 같은 버그 종류라 함께
  고쳤다.
- `RejectsSignatureWithNonZeroUnusedPaddingBits`(Core.Tests)와
  `FromBase64UrlPublicKeys_NonZeroUnusedPaddingBits_ThrowsInvalidTrustedKey`
  (Packager.Tests) 추가 — 둘 다 실제로 존재하는 non-canonical variant(직접 실행해
  찾은, 예: signature 마지막 글자 `w`→`x`, public key 마지막 글자 `8`→`9`)를
  써서 거부되는지 확인한다.

**검증:** 전체 solution `dotnet test` 533개 통과(Core 195→196, Packager 93→94).
두 fix 모두 되돌려서 새 테스트가 실패하는 것을 확인한 뒤 복원했다.

**파일:** `src/GamePatchKit.Core/Signatures/ManifestSignature.cs`,
`src/GamePatchKit.Packager/Base64Url.cs`,
`tests/GamePatchKit.Core.Tests/Signatures/TestManifestSignature.cs`,
`tests/GamePatchKit.Packager.Tests/TestTrustedKeySignatureVerifier.cs`

### ✅ 구현: 12단계 성능·메모리 검증 인프라

**배경:** PRD "성능·관측 기준"이 요구하는 1만 파일·1GiB fixture gate(peak RSS
≤512MiB, 256→1GiB delta ≤64MiB)의 blocking 기준 환경은 Ubuntu 24.04 x64 + GNU
`/usr/bin/time -v`인데, 개발 머신은 macOS ARM64이고 Docker도 없다(macOS
`/usr/bin/time`엔 `-v`가 없음). 사용자가 "인프라만 먼저 구축, 실측은 Linux 준비
후"를 선택해 그 방향으로 진행했다.

**구현:**

- `tests/GamePatchKit.PerformanceTests/Fixtures/PerformanceFixtureGenerator.cs`:
  고정 seed로 1만 파일을 생성한다. 파일 경로·group 배정(`content`=bundle 85%,
  `config`=file 15%)은 totalBytes와 무관하게 항상 같고, 절대 byte 크기만 비례
  조정한다 — 256MiB/1GiB fixture가 같은 파일·seed·group 분포를 공유해야 한다는
  PRD 요구를 그대로 만족한다. marker 파일로 재생성 여부를 판단한다.
- `tests/GamePatchKit.PerformanceTests.Harness/`(신규 Exe): `HttpArtifactTransport`+
  `FileSystemRuntimeStorage`+`PackageRuntime`을 실제 loopback 소켓
  (`System.Net.HttpListener`, 파일 스트리밍)에서 구동하는 별도 프로세스 —
  "Runtime download·활성화" 시나리오 전용. 배포되는 `gpk` CLI에 Runtime/DotNet
  의존성을 넣지 않으려고 분리했다.
- `tests/GamePatchKit.PerformanceTests/Measurement/ProcessRssMeasurement.cs`:
  시나리오를 3회 별도 프로세스로 실행하고 `/usr/bin/time -v`(Linux)/`-l`(macOS,
  참고용)로 peak RSS를 측정. 파서는 고정 샘플 문자열 단위 테스트로 먼저 검증.
- `tests/GamePatchKit.PerformanceTests/Scenarios/`: PRD의 7개 시나리오(최초
  package/incremental/compact/verify/signed verify/plan-download/Runtime
  install)를 실제 `gpk` CLI·harness exe 인자로 구성. 각 시나리오가 쓰는 mutable
  상태(output-root/runtime-root)는 반복마다 pristine golden state에서 새로
  복사해 2·3번째 반복이 "이미 있음" 빠른 경로로 새지 않게 했다(`ScenarioFixtureSet`).
  `GPK_PERF_FULL_SCALE=1`이면 실제 256MiB/1GiB로 전환해 Linux에서만 gate를
  assert한다 — 지금은 스모크 규모(20MiB, 1만 파일)로만 실행 검증했다(7개 전부
  통과, macOS 참고값이 `docs/perf/results.md`에 자동 기록됨).
- `Determinism/TestDeterminismAtScale.cs`: 원래 "병렬도를 바꿔도 결과가 같다"를
  검증하려 했으나, `FilePackageBuilder.cs`가 전부 순차 처리(foreach+await)이고
  조절 가능한 병렬도 옵션이 전혀 없음을 확인했다 — 대신 1만 파일 규모에서 같은
  입력을 두 번 package해 byte가 완전히 같은지(검증 기준 1) 재확인하는 것으로
  범위를 조정했다. 병렬도 옵션이 나중에 생기면 이 테스트를 확장해야 한다.
- `Observability/TestObservabilityMetrics.cs`(카테고리 없음, 일반 테스트에 포함):
  PRD가 요구하는 관측 지표가 대부분 이미 기존 `--json` 출력(`PackageBuildReport`,
  `DownloadPlan`, `StageTimings` 등)에 있음을 확인. 유일한 예외인 "cache hit
  byte"는 discrete 필드가 아니라 `--cache-root` 유무에 따른 `plan.downloadBytes`
  차이로 유도해야 하는 값이라는 걸 테스트로 증명했다 — 새 product 코드는 필요
  없었다.
- `_build/Build.cs`: `Target Test`에 `Category!=Performance` 필터 추가, 신규
  `Target Performance` 추가. `.gitignore`에 fixture 캐시 경로 제외 추가.
- `docs/perf/README.md`(신규), `docs/perf/results.md`(신규, append-only 결과
  표), `docs/plan/12-performance-validation.md` 체크박스 갱신(인프라 항목만
  체크, 실제 Linux gate 실행 항목은 미체크로 남김).

**검증:** 스모크 규모로 `Category=Performance` 전체 실행(7개 시나리오 + fixture
generator 테스트 4개 + RSS 파서 단위 테스트 3개 + determinism 1개) 및
`ObservabilityMetricsTests`를 각각 확인. `dotnet build GamePatchKit.sln`과 신규
두 프로젝트를 포함한 개별 빌드 통과.

**파일:** `tests/GamePatchKit.PerformanceTests/**`,
`tests/GamePatchKit.PerformanceTests.Harness/**`, `_build/Build.cs`,
`.gitignore`, `docs/perf/{README,results}.md`,
`docs/plan/12-performance-validation.md`

### ✅ 12단계 Codex 표준 리뷰 및 수정

**배경:** 12단계 구현 직후 `codex-collab-workflow`(standard review, `gpt-5.6-terra`,
새 thread) 1회차를 돌렸다. High 2건, Medium 1건이 나왔고 전부 재현 가능한 실제
문제였다.

- **[High, 적용]** `GPK_PERF_FULL_SCALE=1`이면 Linux라는 사실만으로 임의의 Linux
  머신 결과를 공식 gate(pass/fail)로 assert하고 있었다 — PRD가 고정한 기준 환경
  (4 vCPU·8GiB RAM·Workstation GC 등)과 실제로 일치하는지는 확인하지 않았다.
  `GPK_PERF_OFFICIAL_GATE=1`을 별도로 추가해 이 값이 켜져 있을 때만(PRD 기준
  환경에 실제 프로비저닝된 CI job만 켜야 함) pass/fail을 assert하도록 분리했다.
- **[High, 적용]** Runtime install harness(`Program.cs`)가 `InstallOrUpdateAsync`의
  `PackageState`와 `IProgress<PatchProgress>`를 버리고 아무 것도 출력하지 않아,
  "PRD 관측 지표가 package·diff·Runtime 결과에 모두 출력된다"는 완료 표시가
  Runtime 쪽은 실제로 검증되지 않은 채 과장돼 있었다. harness가 도달한 `active`
  identity·group 상태·누적 `retryCount`를 CLI와 같은 JSON 관례로 stdout에 출력하게
  고치고, `TestObservabilityMetrics`가 그 출력을 파싱해 검증하도록 확장했다.
- **[Medium, 적용]** `docs/perf/README.md`가 "필터 없는 `dotnet test`는 이 프로젝트를
  건드리지 않는다"고 잘못 적혀 있었다 — `Category!=Performance` 필터는
  `_build/Build.cs`의 Nuke `Test` target에만 있고, `[Trait]`만으로는 필터 없는
  `dotnet test GamePatchKit.sln` 직접 실행에서 제외되지 않는다. 정확한 설명으로
  고쳤다.

**검증:** 세 fix 모두 적용 후 `GamePatchKit.PerformanceTests` 전체(Performance
category 포함, 16개) 재실행 통과, 전체 solution `Category!=Performance` 재실행도
회귀 없음(533개 그대로).

**파일:** `tests/GamePatchKit.PerformanceTests/Scenarios/{ScenarioScale,
TestPerformanceScenarios}.cs`, `tests/GamePatchKit.PerformanceTests.Harness/Program.cs`,
`tests/GamePatchKit.PerformanceTests/Observability/TestObservabilityMetrics.cs`,
`docs/perf/README.md`, `docs/plan/12-performance-validation.md`

### 🔴 미완료 / 다음 창에서 이어갈 일

- `AdapterPath`의 TOCTOU 방어 수준을 더 올릴지(rooted handle 기반) 여부는 사용자 판단
  대기 중이다(07/09부터 이어지는 항목, 아직 미결).
- 10단계 adversarial review는 1회차만 완료했다. 2·3회차는 사용자가 요청하면 진행한다.
- 11단계 adversarial review는 2회차까지 완료(1회차 2건 적용+1건 보류, 2회차 3건
  적용). 2회차의 마지막 보류 항목(`IArtifactTransport` not-found 신호)과
  `/codex:review`의 non-canonical base64url 지적은 이번 세션에서 별도로 요청받아
  구현·해결했다. 3회차 adversarial review는 사용자가 요청하면 진행한다.
- 12단계는 인프라 구축 + Codex 표준 리뷰 1회차(적용 3건, 전부 해결)까지 완료했다.
  실제 Ubuntu 24.04 x64 blocking 환경에서 `GPK_PERF_FULL_SCALE=1
  GPK_PERF_OFFICIAL_GATE=1`로 256MiB/1GiB 실측을 돌려 512MiB/64MiB gate 통과
  여부를 `docs/perf/results.md`에 기록하는 일이 남아 있다.
- 10·11·12단계 모두 아직 커밋하지 않았다.

## 📋 2026-07-27 작업 일지 (작성자 → 다음 세션 전달용)

### ✅ 수정: 12단계 네이티브 `/codex:review` 지적 4건

**배경:** 12단계 성능 검증 인프라에 네이티브 `/codex:review`(리뷰 전용, 어제
Codex 표준 리뷰와는 별개)를 돌렸다. 도중에 한 번은 32분 넘게 진전이 없어
`/codex:cancel` 후 새 thread로 재시작했다(이전에도 겪은, 이 환경의 job이 조용히
멈추는 문제와 동일 패턴). 재시작한 리뷰는 P2 4건을 찾았고 전부 재현 가능한 실제
문제로 확인해 적용했다.

- **[P2, 적용]** `tests/GamePatchKit.PerformanceTests.Harness/Program.cs`:
  `System.Progress<T>`는 콘솔 앱에 캡처된 `SynchronizationContext`가 없어 콜백을
  ThreadPool에 post하고, `InstallOrUpdateAsync`가 리턴돼도 아직 다 안 끝났을 수
  있어 `progressReports`가 비어있거나 stale할 위험이 있었다. `Report()`가 호출
  스레드에서 바로 실행되는 `SynchronousProgress<T>`(직접 구현한 `IProgress<T>`)로
  교체했다.
- **[P2, 적용]** `tests/GamePatchKit.PerformanceTests/AssemblyInfo.cs`(신규):
  xUnit은 기본적으로 서로 다른 테스트 클래스(암묵적 collection)를 병렬로 돌리므로,
  `TestPerformanceScenarios`가 `/usr/bin/time`으로 RSS를 재는 동안
  `TestDeterminismAtScale`/`TestPerformanceFixtureGenerator`가 동시에 package를
  돌리거나 1만 파일을 생성해 측정치를 오염시킬 수 있었다.
  `[assembly: CollectionBehavior(DisableTestParallelization = true)]`로 이
  프로젝트 전체를 순차 실행으로 고정했다.
- **[P2, 적용]** `tests/GamePatchKit.PerformanceTests/Scenarios/TestPerformanceScenarios.cs`:
  full-scale 실행에서 256MiB→1GiB delta를 계산해 pass/fail엔 쓰지만
  `docs/perf/results.md`엔 verdict만 남고 delta 숫자·어느 gate가 실패했는지는
  기록되지 않았다. verdict 문자열에 delta 값과("512MiB peak gate failed"/
  "64MiB delta gate failed") 실패 gate 이름을 포함하도록 고쳤다.
- **[P2, 적용]** `tests/GamePatchKit.PerformanceTests/Fixtures/PerformanceFixtureGenerator.cs`:
  캐시 marker가 `Seed|FileCount|totalBytes`만 봐서, generator 로직(가중치·그룹
  분포·명명 등)만 바뀌고 이 세 값이 그대로면 낡은 fixture를 계속 재사용하는
  위험이 있었다. `GeneratorVersion` 상수를 marker에 포함시켰다.

**검증:** 네 fix 모두 적용 후 `dotnet build` 통과(신규 두 프로젝트),
`GamePatchKit.PerformanceTests` 전체(16개, 순차 실행으로 고정된 뒤) 재실행 통과,
전체 solution `Category!=Performance` 재실행도 회귀 없음(533개 그대로).

**파일:** `tests/GamePatchKit.PerformanceTests.Harness/Program.cs`,
`tests/GamePatchKit.PerformanceTests/AssemblyInfo.cs`(신규),
`tests/GamePatchKit.PerformanceTests/Scenarios/TestPerformanceScenarios.cs`,
`tests/GamePatchKit.PerformanceTests/Reports/PerformanceReportWriter.cs`,
`tests/GamePatchKit.PerformanceTests/Fixtures/PerformanceFixtureGenerator.cs`

### ✅ 수정: 12단계 네이티브 `/codex:review` 2차 지적 5건

**배경:** 위 fix들을 반영한 diff에 `/codex:review`를 한 번 더 돌렸다. 이번에도
`agentmemory/memory_timeline` 호출에서 14분 넘게 멈추는 job을 한 번 겪어 취소 후
재시작했다(같은 patttern, 세 번째). 재시작한 리뷰는 P2 4건 + P3 1건을 찾았고
전부 재현 가능한 실제 문제로 확인해 적용했다.

이 세션 중 `agentmemory-mcp` 서버 프로세스가 40개 넘게(토요일부터 누적) 안 죽고
쌓여있는 것을 발견했다 — 매 Codex 세션마다 `npm exec @agentmemory/mcp`로 새로
뜨는데 정리가 안 되는 것으로 보이고, `memory_recall`/`memory_timeline` 같은
저장소를 훑는 MCP 호출만 유독 멈추는 것과 상관관계가 있어 보인다(단순 `sed`/`rg`
명령은 전혀 안 느려짐). 사용자에게 고아 프로세스 정리 여부를 물었으나 아직
답변받지 않았고, 이번엔 취소·재시작만으로 리뷰 자체는 완료시켰다.

- **[P2, 적용]** `tests/GamePatchKit.PerformanceTests/Scenarios/TestPerformanceScenarios.cs`:
  `ProcessRssMeasurement`가 Linux·macOS만 지원해서 Windows에서 `RunScenarioAsync`를
  부르는 모든 fact가 무조건 예외로 실패했다 — 이 프로젝트가 solution에 포함된
  이상 Windows에서 `dotnet test GamePatchKit.sln`이 항상 깨지는 셈이었다. 함수
  시작부에서 Linux/macOS가 아니면 바로 return하도록 고쳤다.
- **[P2, 적용]** `tests/GamePatchKit.PerformanceTests/Measurement/ProcessRssMeasurement.cs`:
  GNU `time -v`는 non-English 로케일에서 라벨이 번역돼 `ParsePeakRssBytes`의
  영문 매칭이 깨질 수 있었다. 감싸는 프로세스에 `LC_ALL=C`를 강제했다.
- **[P2, 적용]** `tests/GamePatchKit.PerformanceTests/Observability/TestObservabilityMetrics.cs`:
  `AssertRuntimeMetrics`가 `retryCount` 필드 존재만 확인해서, progress가 한 번도
  안 와도(harness가 기본값 0을 채우므로) 통과했다 — stage·file·byte 진행 정보는
  전혀 검증하지 않았다. `reportCount > 0`을 assert하고 harness가 내보내는
  `lastStage`/`lastTotalFiles`/`lastTotalBytes`도 확인하도록 확장했다.
- **[P2, 적용]** `tests/GamePatchKit.PerformanceTests.Harness/Program.cs`:
  `PackageRuntime.cs`를 직접 확인한 결과 `PatchProgress.RetryCount`는 artifact
  객체마다(`DownloadObjectAsync` 호출마다) 0으로 리셋되는 값이었다 — 그래서
  `Max()`로 집계하면 여러 객체가 각각 한 번씩 재시도해도 "가장 많이 재시도한
  객체 하나"만 보이고 나머지는 사라졌다. distinct한 `(RelativePath, RetryCount>0)`
  쌍의 개수로 바꿔 실제 총 재시도 횟수를 세도록 고쳤다.
- **[P3, 적용]** `tests/GamePatchKit.PerformanceTests/Scenarios/TestPerformanceScenarios.cs`:
  smoke 규모 실행에서 verdict를 명시적으로 넘기지 않아, Linux 개발 머신에서
  smoke를 돌리면 `PerformanceReportWriter`의 기본 fallback이 "reference only"가
  아니라 "not asserted"를 내서 문서 규칙과 안 맞았다(macOS에서만 테스트해서 못
  봤던 문제). smoke 호출에 `"reference only (smoke scale)"`를 명시적으로 넘기도록
  고쳤다.

**검증:** 다섯 fix 모두 적용 후 `dotnet build` 통과, `GamePatchKit.PerformanceTests`
전체 재실행 통과.

**파일:** `tests/GamePatchKit.PerformanceTests/Scenarios/TestPerformanceScenarios.cs`,
`tests/GamePatchKit.PerformanceTests/Measurement/ProcessRssMeasurement.cs`,
`tests/GamePatchKit.PerformanceTests/Observability/TestObservabilityMetrics.cs`,
`tests/GamePatchKit.PerformanceTests.Harness/Program.cs`

### ✅ 수정: 12단계 네이티브 `/codex:review` 3차 지적 3건 + `agentmemory-mcp` 고아 프로세스 정리

**배경:** 위 fix들을 반영한 diff에 `/codex:review`를 세 번째로 돌렸다. 이번엔
멈추지 않고 정상적으로 끝났다 — 그 전에 사용자 요청으로 `agentmemory-mcp` 고아
프로세스를 정리한 게 영향을 준 것으로 보인다(이 세션의 codex 공유 런타임
PPID 72823 밑에 쌓여 있던 16쌍/32개 프로세스를 종료했고, 다른 터미널 세션에
속한 나머지 28개는 그대로 뒀다). P1 1건 + P2 2건이 나왔고 전부 재현 가능한 실제
문제로 확인해 적용했다.

- **[P1, 적용]** `tests/GamePatchKit.PerformanceTests/Scenarios/TestPerformanceScenarios.cs`:
  `GPK_PERF_OFFICIAL_GATE=1`인데 `GPK_PERF_FULL_SCALE`이 빠지거나 잘못 설정되면,
  smoke 실행 후 조용히 return해서 실제 fixture 측정도 gate assert도 전혀 없이
  7개 blocking test가 전부 통과했다 — CI가 공식 gate를 돈다고 착각한 채로 green을
  받을 수 있는 심각한 문제였다. 이 모순된 조합을 명시적으로 `Assert.False`로
  실패시키도록 고쳤다. `GPK_PERF_OFFICIAL_GATE=1`만 주고(`GPK_PERF_FULL_SCALE`
  없이) 실제로 fail하는지 직접 실행해 확인했다.
- **[P2, 적용]** `tests/GamePatchKit.PerformanceTests.Harness/Program.cs`:
  `PackageRuntime.cs`를 다시 확인한 결과 마지막 progress report는 항상
  `new PatchProgress(PatchStage.Completed)`(다른 인자 전부 생성자 기본값 0)라서,
  `progressReports[^1]`을 그대로 쓰면 `lastCompletedFiles`/`lastTotalFiles`/
  `lastCompletedBytes`/`lastTotalBytes`가 실제 다운로드가 있어도 항상 0이었다.
  실제 합계를 담은 마지막 report(`TotalFiles > 0 || TotalBytes > 0`)를 따로 찾아
  그 값을 쓰도록 고치고, `TestObservabilityMetrics`의 검증도 `NotNull`이 아니라
  `> 0`으로 강화했다(그래야 이 종류의 회귀를 실제로 잡는다).
- **[P2, 적용]** `tests/GamePatchKit.PerformanceTests/Fixtures/PerformanceFixtureGenerator.cs`:
  생성되는 `gamepatchkit.yml`은 절대경로 `inputRoot`를 그대로 박아 넣는데, 캐시
  marker는 그 경로와 무관해서 checkout이 다른 절대경로로 옮겨지거나 복원되면
  낡은(다른 경로를 가리키는) config를 그대로 재사용하는 위험이 있었다. marker에
  `inputRoot`를 포함시켰다.

**검증:** 세 fix 모두 적용 후 `GamePatchKit.PerformanceTests` 전체(16개) 재실행
통과, 전체 solution `Category!=Performance` 재실행도 회귀 없음(533개 그대로).
P1 fix는 `GPK_PERF_OFFICIAL_GATE=1`만 주고 실행해 실제로 의도한 메시지로
fail하는 것까지 직접 확인했다.

**파일:** `tests/GamePatchKit.PerformanceTests/Scenarios/TestPerformanceScenarios.cs`,
`tests/GamePatchKit.PerformanceTests.Harness/Program.cs`,
`tests/GamePatchKit.PerformanceTests/Observability/TestObservabilityMetrics.cs`,
`tests/GamePatchKit.PerformanceTests/Fixtures/PerformanceFixtureGenerator.cs`

### ✅ 수정: 12단계 네이티브 `/codex:review` 4차 지적 3건 (사용자가 "문제 없을 때까지 반복" 요청)

**배경:** 사용자가 리뷰→수정을 문제가 없어질 때까지 반복해달라고 요청해 자동
루프로 전환했다. 4차 리뷰에서도 한 번 `agentmemory/memory_lesson_recall`에서
멈추는 job을 겪어 이 세션이 새로 띄운 orphan(PPID 기준, 이번엔 17689/77501 밑
4쌍/8개)을 정리하고 취소·재시작했다. 재시작한 리뷰는 P1 1건 + P2 2건을 찾았다.

- **[P1, 적용]** `tests/GamePatchKit.PerformanceTests/Scenarios/TestPerformanceScenarios.cs`:
  3차에서 고친 "OfficialGate면 FullScale 강제"만으로는 부족했다 —
  `GPK_PERF_FULL_SCALE=1`과 `GPK_PERF_OFFICIAL_GATE=1`을 둘 다 켜도 **macOS**에서
  돌리면 `isOfficialGateRun`이 `IsAuthoritative`(Linux 여부)에 의해 false가 되어
  "reference only"로 빠지고 gate assert가 전혀 안 되는 구멍이 남아있었다. 함수
  맨 앞에서 `OfficialGate`면 `ChildProcess.IsLinux`와 `FullScale`을 둘 다 강제
  assert하도록 재구성했다 — Windows 조기 return과 macOS "reference only" fallback
  둘 다 이 체크보다 뒤에 오게 만들어 어떤 경로로도 새지 않게 했다. macOS에서
  `GPK_PERF_OFFICIAL_GATE=1`(단독)과 `GPK_PERF_OFFICIAL_GATE=1
  GPK_PERF_FULL_SCALE=1`(둘 다) 두 조합 모두 직접 실행해 의도한 메시지로 즉시
  fail하는 것을 확인했다.
- **[P2, 적용, product 코드]** PRD가 요구하는 "생성·재사용 artifact 수·byte"에서
  재사용(reused) 쪽 byte가 실제로 어디에도 없었다 — `PackageBuildReport`에
  `ReusedFileArtifactCount`/`ReusedBundleArtifactCount`(개수)만 있고 대응하는
  byte 필드 자체가 product 모델에 없었다(테스트만의 문제가 아니었다).
  `FilePackageBuilder.BuildState`에 `ReusedFileArtifactBytes`/`ReusedBundleBytes`
  누적 필드를 추가하고(`HashSet.Add`의 반환값으로 같은 artifact가 여러 파일에서
  재사용돼도 byte가 중복 집계되지 않게 함), `PackageBuildReport`·
  `PackageCommand`의 JSON/텍스트 출력까지 다 연결했다.
  `TestFilePackageBuilder`의 기존 재사용 테스트 2개(파일 재사용·bundle 재사용)에
  byte 검증을 추가하고, 파일 쪽은 되돌려서 실제로 실패하는 것(기대 13, 실제 0)을
  확인한 뒤 복원했다.
- **[P2, 적용]** `tests/GamePatchKit.PerformanceTests/Observability/TestObservabilityMetrics.cs`:
  테스트 이름이 "모든 PRD 필수 지표"를 검증한다고 주장하면서 `compact`를 한 번도
  안 불러서, "compact 전후 신규 설치 byte 차이"를 계산·기록하는 부분이 아예
  없었다. `compact`를 호출하고(재사용된 "content" bundle group 대상)
  `plan-download --required-only`를 compact 전/후 manifestHash 각각에 대해 호출해
  fresh-install byte 추정치를 비교하는 방식으로 이 값이 유도 가능함을 증명하는
  단계를 추가했다.

**검증:** 세 fix 모두 적용 후 `GamePatchKit.PerformanceTests` 전체(16개) 재실행
통과, `GamePatchKit.Packager.Tests` 전체(94개) 재실행 통과, 전체 solution
`Category!=Performance` 재실행도 회귀 없음(533개 그대로). P1은 두 조합
(`OFFICIAL_GATE`만 / `OFFICIAL_GATE`+`FULL_SCALE` 둘 다) 모두 실제 실행해 확인.

**파일:** `tests/GamePatchKit.PerformanceTests/Scenarios/TestPerformanceScenarios.cs`,
`src/GamePatchKit.Packager/{FilePackageBuilder,PackageBuildReport}.cs`,
`src/GamePatchKit.Cli/Commands/PackageCommand.cs`,
`tests/GamePatchKit.Packager.Tests/TestFilePackageBuilder.cs`,
`tests/GamePatchKit.PerformanceTests/Observability/TestObservabilityMetrics.cs`

### ✅ 수정: 12단계 네이티브 `/codex:review` 5차 지적 2건

**배경:** 5차 리뷰는 `agentmemory` 호출에서 두 번 멈춰서(각각 다른 도구:
`memory_lesson_recall`, `memory_smart_search`) 이 세션 orphan을 두 번 정리·재시작한
끝에 완료됐다. 사용자가 agentmemory 호출을 아예 막을 방법을 물어봐서
`~/.codex/config.toml`의 `[mcp_servers.agentmemory]`(전역, 원격 서버
`amconnect.ooustudio.com` 사용)를 찾아 설명했다 — 전역 설정이라 건드리지 않고
지금처럼 멈추면 정리·재시작하는 방식을 계속 쓰기로 했다. 리뷰 자체는 P1 1건 +
P2 1건을 찾았다.

- **[P1, 적용]** `tests/GamePatchKit.PerformanceTests.Harness/StaticFileHttpListener.cs`:
  `HttpListener`는 Windows에서 HTTP.sys 기반이라 관리자 권한이 없거나 URL ACL
  예약이 없으면 동적으로 고른 loopback prefix에 대해 `Access is denied`로 막힌다.
  `TestObservabilityMetrics`는 Performance category가 아니라 **일반
  `dotnet test`에 포함**되는데 이 harness를 무조건 실행하므로, Windows에서
  일반 테스트가 깨질 수 있었다. `HttpListener`를 걷어내고 `TcpListener` 기반
  최소 HTTP/1.1 GET 서버(요청 줄만 파싱, 헤더는 버림, `Content-Length`/
  `Connection: close`만 응답)로 새로 짰다 — 어떤 플랫폼에서도 권한 상승이
  필요 없다. `TestObservabilityMetrics`로 실제 파일 전송·`InstallOrUpdateAsync`가
  새 서버로 정상 동작하는 것을 확인했다.
- **[P2, 적용]** `tests/GamePatchKit.PerformanceTests/Observability/TestObservabilityMetrics.cs`:
  compact가 조용히 no-op이 되거나 `compactedHash`가 실수로 `incrementalHash`와
  같아져도 두 `downloadBytes` assert(둘 다 `> 0`)는 그대로 통과해서, "compact
  전후 비교"를 실제로 검증하지 못했다. `compactResult.changed == true`와
  `compactedHash != incrementalHash`를 먼저 assert하도록 추가했다 —
  `compactedHash`를 `incrementalHash`로 바꿔서 되돌린 뒤 실제로 실패하는 것
  (`NotEqual` 실패)을 확인하고 복원했다.

**검증:** 두 fix 모두 적용 후 `TestObservabilityMetrics` 단독 재실행 통과,
`GamePatchKit.PerformanceTests` 전체 재실행 진행 중(다음 항목에 결과 기록 예정).

**파일:** `tests/GamePatchKit.PerformanceTests.Harness/StaticFileHttpListener.cs`,
`tests/GamePatchKit.PerformanceTests/Observability/TestObservabilityMetrics.cs`

### ✅ 수정: 12단계 네이티브 `/codex:review` 6차 지적 2건

**배경:** 6차 리뷰는 `agentmemory` 호출에서 세 번 멈춰(각각 정리·재시작) 네 번째
시도에서 완료됐다. P1 2건이 나왔는데, 둘 다 "골든 상태나 measured 시나리오가
조용히 더 쉬운/작은 작업으로 바뀌어도 테스트가 눈치채지 못하는" 같은 종류의
문제였다.

- **[P1, 적용]** `tests/GamePatchKit.PerformanceTests/Scenarios/ScenarioFixtureSet.cs`:
  golden package 결과에서 manifestHash/dataVersion만 뽑아 쓰고 `totals.fileCount`/
  `totals.fileBytes`가 요청한 fixture 크기(`PerformanceFixtureGenerator.FileCount`/
  `totalBytes`)와 실제로 맞는지 확인하지 않았다 — config나 glob 회귀로 파일이
  조용히 누락돼도 `gpk package`는 성공(exit 0)하므로, 이후 모든 시나리오가
  의도보다 작은 workload를 "진짜 fixture"인 것처럼 측정·인증할 수 있었다.
  `AssertMatchesRequestedFixtureSize`를 추가해 골든 package 직후 바로 확인한다.
  일부러 기대값을 어긋나게 만들어 실제로 실패하는 것(기대 10000/20971520,
  요청한 값과 다르게)을 확인한 뒤 복원했다.
- **[P1, 적용]** `tests/GamePatchKit.PerformanceTests/Scenarios/TestPerformanceScenarios.cs`:
  `Compact` 시나리오가 stdout을 버려서, incremental golden state가 나중에
  "compact할 게 없는" 상태로 바뀌어도(`changed=false`인 no-op) RSS 측정 자체는
  그대로 성공해 값을 기록·gate 판정에 쓸 수 있었다 — 실제로는 아무 일도 안 한
  값이 공식 gate를 통과시킬 수 있는 셈이었다. RSS로 측정하지 않는 별도 preflight
  compact 호출을 추가해 `changed == true`를 먼저 확인하도록 했다(측정 3회 반복에는
  안 들어가는 완전히 별개의 실행). `buildScenario`의 시그니처를
  `Func<SizeState, Func<ProcessStartInfo>>`에서 `Func<SizeState, Task<Func<ProcessStartInfo>>>`로
  바꿔 preflight를 `await`할 수 있게 했고, 나머지 6개 시나리오는
  `Task.FromResult(...)`로 감싸 변경 없이 맞췄다. 실제 `Compact` 테스트를 돌려
  preflight가 정상 동작하는 것을 확인했다.

**검증:** 두 fix 모두 적용 후 `TestPerformanceScenarios.InitialPackage`·`Compact`
개별 재실행 통과, `GamePatchKit.PerformanceTests` 전체 재실행 진행 중(다음 항목에
결과 기록 예정).

**파일:** `tests/GamePatchKit.PerformanceTests/Scenarios/{ScenarioFixtureSet,
TestPerformanceScenarios}.cs`

### 🔴 미완료 / 다음 창에서 이어갈 일

- `AdapterPath`의 TOCTOU 방어 수준을 더 올릴지(rooted handle 기반) 여부는 사용자 판단
  대기 중이다(07/09부터 이어지는 항목, 아직 미결).
- 10단계 adversarial review는 1회차만 완료했다. 2·3회차는 사용자가 요청하면 진행한다.
- 11단계 adversarial review는 2회차까지 완료. 3회차는 사용자가 요청하면 진행한다.
- 12단계는 인프라 구축 + Codex 표준 리뷰 1회차 + 네이티브 `/codex:review` 6회
  (총 지적 19건, 전부 적용)까지 완료했다. 사용자 요청으로 "문제 없을 때까지"
  리뷰→수정을 자동 반복하는 중이다. 실제 Ubuntu 24.04 x64 blocking 환경에서
  `GPK_PERF_FULL_SCALE=1 GPK_PERF_OFFICIAL_GATE=1`로 256MiB/1GiB 실측을 돌려
  512MiB/64MiB gate 통과 여부를 `docs/perf/results.md`에 기록하는 일은 여전히
  남아 있다.
- `agentmemory-mcp` 호출(특히 `memory_lesson_recall`/`memory_smart_search`)이
  반복적으로 멈추는 문제는 여전히 못 고쳤다 — 원인은 `~/.codex/config.toml`의
  `AGENTMEMORY_URL`이 원격 서버(`amconnect.ooustudio.com`)라 네트워크/원격 쪽
  지연일 가능성이 높다. 전역 설정이라 건드리지 않기로 했고, 멈출 때마다 이
  세션이 새로 띄운 orphan만 정리하고 취소·재시작하는 방식을 계속 쓴다.
- 10·11·12단계 모두 아직 커밋하지 않았다.

### ✅ 수정: agentmemory CLI·MCP/core 0.9.28 통일 및 Codex 프록시 설정 보정

**증상/배경:** Claude의 Codex 플러그인에서 `memory_insight_list` 호출이 완료되지
않았고, 실제 설치 상태를 확인하니 전역 CLI는 `0.9.27`, MCP wrapper는 `0.9.28`,
wrapper가 `npx` 캐시에서 로딩한 core는 `0.9.25`로 서로 달랐다.

**수정:**

- 전역 `@agentmemory/agentmemory`와 `@agentmemory/mcp`를 모두 `0.9.28`로 설치했다.
- `~/.codex/config.toml`의 MCP command를 오래된 `npx` 캐시 대신
  `/Users/tteogi/.local/share/mise/shims/agentmemory-mcp`로 변경했다.
- 원격 `AGENTMEMORY_URL`의 `/agentmemory/livez`가 404를 반환해 0.9.28 shim이
  7-tool local fallback으로 전환하는 문제를 확인하고
  `AGENTMEMORY_FORCE_PROXY=1`을 추가했다.
- 기존 Claude Codex plugin broker를 공식 `SessionEnd` 경로로 종료해 다음 리뷰가
  새 설정으로 broker를 생성하게 했다. 다른 앱의 기존 MCP 프로세스는 해당 앱을
  재시작할 때 교체되도록 남겼다.
- `memory_insight_list` 비활성화와 30초 timeout은 안전을 위해 유지했다.

**검증:** 전역 dependency tree에서 MCP/core가 `0.9.28`로 dedupe된 것을 확인했다.
새 shim의 MCP initialize 응답은 `serverInfo.version=0.9.28`이었고,
`AGENTMEMORY_FORCE_PROXY=1` 상태에서 `memory_insight_list(project=gamepatchkit)`가
즉시 `success: true`로 완료됐다. 원격 backend는 health API에 버전을 노출하지 않아
CLI에서 `v?`로 표시되지만, 실제 MCP 프록시 호출 호환성은 확인했다.

**파일:** `~/.codex/config.toml`, `docs/worklog/2026-07.md`

### 🔎 리뷰: 전체 구현·Unity Runtime 통합 점검

**범위:** `develop`의 Core, Compression, Packager, CLI, Runtime, DotNet,
Conformance, signing, 성능·배포 문서와 PRD 검증 기준 1~26을 통합 검토했다.

**결과:**

- Release 비성능 테스트 537개는 모두 통과했다.
- 공식 `./build.sh Test`는 Nuke의 `_solution` binding 실패로 Restore 전에
  중단된다.
- zstd decoder가 정상 frame 뒤 trailing byte를 거부하지 않는 재현 가능한 공백을
  확인했다.
- build dependency `System.Security.Cryptography.Xml 10.0.9`에 High advisory
  5개가 있으며 10.0.10 이상으로 갱신해야 한다.
- Ubuntu 24.04 x64 1GiB 공식 성능 gate와 13단계 문서·배포 작업은 아직 미완료다.
- Unity 6000.4.4f1 macOS Mono 탐색성 smoke에서 Core/Runtime 무압축 1-file 설치는
  성공했다. 다만 UnityWebRequest·persistentDataPath adapter, IL2CPP, Android/iOS/
  WebGL 및 Unity conformance는 검증되지 않아 공식 Unity 지원으로 판정하지 않았다.

**판정:** 조건부 통과, release 보류. P0는 Nuke build 복구, 취약 dependency 갱신,
zstd strict decoding과 회귀 테스트다. 그 다음 Unity 최소 지원 matrix와 재현 가능한
Unity fixture, Linux 공식 성능 gate를 완료한다.

**파일:** `docs/review/integrated-review-2026-07-27.md`

### ✅ 구현: Unity Runtime adapter와 재현 가능한 Unity 6.4 검증 프로젝트

**배경:** Unity 엔진에서 `GamePatchKit.Runtime`을 실제로 사용할 수 있는지 확인하기
위해, 삭제된 임시 Unity 프로젝트 대신 저장소 안에 재현 가능한 검증 프로젝트와
Unity 전용 adapter가 필요했다.

**구현:**

- `unity/GamePatchKit.Unity`에 Unity `6000.4.4f1` 프로젝트와 embedded UPM package를
  추가했다.
- `UnityWebRequestArtifactTransport`가 `DownloadHandlerFile`로 manifest·artifact를
  임시 파일에 streaming하고, HTTP 404·408·429·5xx와 caller cancellation을 Runtime
  계약에 맞게 분류하도록 구현했다.
- `UnityRuntimeStorage`가 `Application.persistentDataPath/GamePatchKit` 아래 writer
  lock, canonical state 원자 교체, cache commit, staging promotion, installation
  stream을 제공하도록 구현했다.
- Core·Runtime·BouncyCastle managed plugin은 커밋하지 않고 `prepare.sh`가
  Runtime 프로젝트를 restore/build한 뒤 복사하도록 했다. Newtonsoft.Json과
  UnityWebRequest 의존성은 UPM package 자체에 선언했다.
- macOS Standalone의 scripting backend를 IL2CPP로 고정하고, 동일 Editor 버전의
  `Mac Build Support (IL2CPP)` Player variation이 없으면 build script가 즉시
  설명 가능한 오류로 중단하도록 했다.
- 전송·취소·오류 분류, 저장소 원자성·잠금·경로, 실제 loopback HTTP →
  `PackageRuntime` → cache/staging/install E2E를 포함한 Unity EditMode 테스트
  12개를 추가했다.
- 사용법과 첫 지원 범위를 `docs/contracts/unity-adapter.md`에 기록했다.

**검증:**

- `./unity/GamePatchKit.Unity/scripts/test.sh`: Unity EditMode 12/12 통과
- `dotnet test GamePatchKit.sln --no-restore`: 549/549 통과
- C# 컴파일 경고 0, JSON/asmdef parse와 shell syntax 검사 통과

**파일:** `unity/GamePatchKit.Unity/**`,
`docs/design/unity-runtime-integration-design.md`,
`docs/contracts/unity-adapter.md`, `README.md`

### 🔴 미완료 / 다음 창에서 이어갈 일: macOS IL2CPP Player build

- Unity Hub는 `6000.4.4f1`용 `Mac Build Support (IL2CPP)` 다운로드를 시도했지만
  `getaddrinfo ENOTFOUND wpad`로 0 byte에서 실패했다. 현재 해당 Editor의
  `MacStandaloneSupport/Variations`에는 Mono Player만 있어 Unity도
  `Currently selected scripting backend (IL2CPP) is not installed`로 빌드를
  거부한다.
- Unity Hub에서 **6000.4.4f1의 `Mac Build Support (IL2CPP)`** 설치를 재시도한 뒤
  `./unity/GamePatchKit.Unity/scripts/build-macos-il2cpp.sh`를 실행해야 한다.
- 성공 기준은 `Build/macOS/GamePatchKitUnitySmoke.app` 생성과
  `GAMEPATCHKIT_UNITY_MACOS_IL2CPP_BUILD_OK` 로그 확인이다.

## 📋 2026-07-28 작업 일지 (작성자 → 다음 세션 전달용)

### ✅ 구현: macOS IL2CPP Player Test 최소 구성

- `Packages/com.sidenation.gamepatchkit.unity/Tests/Runtime`에 Player 전용
  `GamePatchKit.Unity.PlayerTests` asmdef와 IL2CPP settings JSON을 추가했다.
- `TestPlayerRuntimeSmoke` 1건은 실제 Player에서 `Application.isEditor == false`,
  `UnityRuntimeStorage` 상태 저장·조회, loopback HTTP를 통한
  `UnityWebRequestArtifactTransport` artifact 다운로드와 임시 파일 정리를 검증한다.
- Test-run 완료 상태는 Player log의
  `GAMEPATCHKIT_UNITY_PLAYER_TEST_RESULT:<status>` marker로도 확인할 수 있다.
  이 callback은 결과만 기록하며 Player를 강제 종료하지 않아 Editor 결과 전달을
  방해하지 않는다.

### ✅ 검증

- Unity 6000.4.4f1 Test Runner가 `StandaloneOSX`/`IL2CPP`로 Player test assembly를
  컴파일하고 PlayerWithTests app을 생성했다 (`Build Finished, Result: Success`).
- 생성된 동일 Player를 `-batchmode -nographics`로 실행해 실제 IL2CPP 런타임
  스모크 테스트의 `GAMEPATCHKIT_UNITY_PLAYER_TEST_RESULT:Passed`를 확인했다.
- Unity EditMode Test Runner는 기존 12/12 통과했다.

### 🗒️ 다음 실행 시 유의점

- 이 터미널 환경에서 일반 그래픽 Player는
  `GfxDeviceMetal::WaitForLastPresentationAndGetTimestamp`에서 표시 신호를 기다려
  Test Runner 결과를 Editor에 보내지 못한다. 헤드리스 fallback은
  `PlayerWithTests.app/Contents/MacOS/GamePatchKit.Unity -batchmode -nographics`다.
- GUI가 가능한 macOS CI/데스크톱에서는 정상 `-runTests -testPlatform StandaloneOSX`
  실행으로 Editor XML 결과 전달까지 다시 확인한다.
- `-runTests`와 `-quit`을 함께 전달하면 import 후 종료되어 테스트가 실행되지
  않으므로 함께 쓰지 않는다.

**파일:** `unity/GamePatchKit.Unity/Packages/com.sidenation.gamepatchkit.unity/Tests/Runtime/{GamePatchKit.Unity.PlayerTests.asmdef,TestPlayerRuntimeSmoke.cs,StandaloneOsxIl2CppTestSettings.json}`,
`docs/worklog/2026-07.md`

### ✅ 구현: 13단계 문서화·배포 산출물 정리 (마지막 단계)

**배경:** `docs/plan/13-documentation.md`의 모든 항목이 미완료였고, 통합 리뷰 R7이
"README만으로는 적용 불가"로 지적한 상태였다.

**문서:** README를 전면 재작성하고(개요·설치·빠른 시작 7단계·핵심 개념·CLI 요약·문서
지도), 세부는 `docs/guide/` 7종으로 나눴다 — `quickstart`, `package-config`,
`identity`, `runtime-integration`, `unity`, `publishing`, `distribution`. 13단계
작업 항목 22개가 어디에 들어갔는지는 계획 문서에 매핑해 뒀다.

**샘플:** `samples/quickstart`를 추가했다. `run.sh` 하나가 package → verify → sign →
Runtime 설치 → incremental → compact를 순서대로 실행하고, `QuickStartClient`가
`HttpArtifactTransport` + `FileSystemRuntimeStorage` + `PackageRuntime` 조립을 보여준다.
샘플이 조용히 낡지 않도록 `QuickStartClient`를 solution에 넣었다.

**결정 사항:**

- adapter conformance suite 배포 형태를 **소스 fixture 유지**로 확정했다(10단계가
  13단계로 미뤄 뒀던 항목). NuGet test package로 만들면 소비자의 테스트 프레임워크를
  강제하게 되고, Unity처럼 xUnit을 그대로 못 돌리는 host는 어차피 시나리오를 옮겨야
  한다.
- JSON Schema 3종은 별도 패키지·host URL 없이 **repository release에 포함**하고,
  `$id`가 파일명뿐인 상대 참조라 원격 fetch 대상이 아님을 명시했다.
- `gpk`를 Pack/Push 대상에 추가해 `.NET tool`로 실제 배포 가능하게 했다.
- `LICENSE` placeholder를 MIT 전문으로 채워 `PackageLicenseExpression=MIT` 선언과
  일치시켰다.

### ✅ 수정: 공식 빌드 진입점이 동작하지 않던 문제 (통합 리뷰 R1)

**증상:** `./build.sh Restore`가 `Could not inject value for Build._solution` 경고 뒤
`NullReferenceException`으로 죽었다. README가 안내하던 `--version`,
`--nuget-api-key`도 실제로는 `--_version`, `--_-nuget-api-key`로 노출돼 있었다.

**원인:** Nuke는 주입 대상을 **멤버 이름**으로 찾는데 `_build/Build.cs`의 parameter
필드가 저장소 규칙대로 `_camelCase`였다. `.nuke/parameters.json`의 `"Solution"`이
`_solution`과 매칭되지 않았다.

**수정:** 해당 필드들만 Nuke 관례인 PascalCase로 바꾸고 이유를 코드 주석으로 남겼다.
이제 `--configuration`, `--nuget-api-key`, `--nuget-source`, `--version`이 정상
노출되고 `Restore`/`Compile`/`Test`/`Pack`/`Push`가 전부 동작한다.

**파일:** `_build/Build.cs`

### ✅ 수정: 빌드 의존성 High severity 취약점 (통합 리뷰 R5)

`System.Security.Cryptography.Xml` 10.0.9 → 10.0.10. 매 빌드마다 뜨던 NU1903 경고
5개가 사라졌다. 제품 런타임 의존성이 아니라 build project 전용이다.

**파일:** `Directory.Packages.props`

### ✅ 검증

- `./build.sh Test` — 9개 test project 537개 통과, NU1903 경고 0개.
- `./build.sh Pack --version 0.1.0` — **경고 없이** nupkg 5종 생성(4 라이브러리 +
  gpk tool). 이전에 4종 모두 나던 "package README 없음" 경고가 사라졌다.
- 로컬 artifacts에서 `dotnet tool install`로 `gpk` 설치 후 README 빠른 시작 1~7단계를
  빈 디렉터리에서 그대로 실행 — publish tree 배치, `manifest.sig` 225 byte,
  `signature.state: verified`, Runtime이 required→optional 순서로 설치해
  `stateRevision` 1→2, 설치 파일 내용 일치(zstd 해제 + bundle 추출 포함)까지 확인.
- glob 문서(검증 기준 26): `**/*`가 root 파일을 포함하고 숨김 segment는 암묵적으로
  일치시키지 않는 것, `.`으로 시작하는 pattern을 명시하면 잡히는 것을 실제 실행으로 확인.
- 결정성: 같은 source로 두 번 package해 `manifestHash` 동일. `--dry-run`은 실제
  identity를 보고하면서 output tree를 byte 단위로 보존.
- 저장소 markdown 48개의 상대 링크·anchor 전부 유효.

### 🔴 남은 일

- **Ubuntu 24.04 x64 공식 성능 gate(PRD 검증 기준 17)는 여전히 미실행이다.** 13단계는
  기준과 실행 절차를 문서로 고정했을 뿐 수치를 만들지 않는다. 실행 명령은
  `docs/guide/distribution.md`와 `docs/perf/README.md`에 있다.
- 통합 리뷰의 나머지 항목은 그대로 남아 있다: R4(zstd trailing byte),
  R6(`Pack`이 `Directory.Build.props`의 version을 실행 중 변경), R8(CI 부재,
  `dotnet format` 미적용). 전부 13단계 범위 밖이라 손대지 않았다.
- Unity는 `compression: none` + macOS Standalone까지만 지원 판정이다. Windows·Android·
  iOS·WebGL은 미검증.

**파일:** `README.md`, `docs/guide/{quickstart,package-config,identity,runtime-integration,unity,publishing,distribution}.md`,
`samples/{README.md,quickstart/**}`, `src/*/README.md`(5종), `Directory.Build.props`,
`Directory.Packages.props`, `_build/Build.cs`, `LICENSE`, `GamePatchKit.sln`,
`.gitignore`, `docs/plan/{README.md,13-documentation.md}`,
`docs/contracts/adapter-conformance.md`

## 📋 2026-07-29 작업 일지 (작성자 → 다음 세션 전달용)

### 오늘 한 것

- Unity용 QuickStart 가이드(`docs/guide/unity-quickstart.md`)와 그 서버 쪽 샘플
  (`samples/unity-quickstart/`)을 추가했다. 13단계 문서 세트의 마지막 보강이다.

### ✅ 구현: Unity 10분 QuickStart

**배경:** `guide/unity.md`는 "내 프로젝트에 제대로 붙이는 절차"라 처음 보는 사람이
동작을 확인하기까지 읽어야 할 양이 많다. "일단 Play를 눌러 데이터가 설치되는 것까지"만
보는 최단 경로를 따로 뒀다.

**만든 것:**

- `docs/guide/unity-quickstart.md` — 준비물 → `prepare.sh` → `serve.sh` → 프로젝트 열기
  → 컴포넌트 붙이고 Play → Console·저장 위치 확인. Unity에서만 나는 실패를 모은 표
  (main thread 생성, base URL의 끝 `/`, 404, `runtime.missing-compression-codec`,
  loopback이 아닌 `http://` 차단, `link.xml` 누락, Newtonsoft 중복, `async void`가
  삼킨 예외)와 다음에 읽을 문서 표.
- `samples/unity-quickstart/{gamepatchkit.yml,serve.sh,README.md}` — `serve.sh`가
  `compression: none` release를 만들고 Inspector에 넣을 네 값(Base Url·Package Id·
  Data Version·Manifest Hash)을 출력한 뒤 publish tree를 loopback으로 서빙한다.
  게임 데이터는 `samples/quickstart/game-data`를 재사용하고, 설정은 quickstart 샘플과
  `compression: none` 하나만 다르다. 생성물은 `.work/`에만 만든다.
- `unity/GamePatchKit.Unity/Assets/PatchQuickStart.cs` — 문서가 인용하는 client
  스크립트. **Unity 검증 프로젝트의 `Assets/`에 둔 이유는** `scripts/test.sh`가 돌 때
  같이 컴파일돼 문서 코드가 조용히 낡지 않게 하기 위해서다(`QuickStartClient`를
  solution에 넣은 것과 같은 이유).

**결정:** `serve.sh`는 `--bind 127.0.0.1`로 loopback에만 연다. Unity의
`insecureHttpOption`이 이 프로젝트 기준 `NotAllowed(0)`인데 loopback은 그 제한을 받지
않기 때문이다. 다른 기기에서 붙이려면 Player Settings를 먼저 바꿔야 한다는 것을 문서와
샘플 README 양쪽에 적었다.

### 검증

- `./samples/unity-quickstart/serve.sh` — release 생성·값 출력·서빙 성공.
  `http://127.0.0.1:8080/unity-sample-data/manifests/<hash>/manifest.json`(Unity
  transport가 만드는 바로 그 경로)이 200을 반환.
- **codec을 하나도 주입하지 않은** client로 같은 서버에서 설치 — required 후
  `stateRevision: 1`, optional `maps` 후 2,
  `OpenInstallationFileAsync(installationKey, "core/config.json")`가 원본과 같은 내용을
  반환. Unity 구성(codec 없음 + `compression: none`)이 실제로 성립하는지 확인한 것이다.
- `./unity/GamePatchKit.Unity/scripts/test.sh` — `PatchQuickStart.cs`를 넣은 상태에서
  EditMode 12개 전부 통과, `error CS` 0건.

### 🔴 남은 일

- 2026-07-28 항목의 남은 일이 그대로 유효하다: Ubuntu 24.04 x64 공식 성능 gate 미실행,
  통합 리뷰 R4·R6·R8 미처리, Unity 지원 범위는 `compression: none` + macOS Standalone.
- Unity Play mode에서 `PatchQuickStart`를 실제로 눌러 본 것은 아니다. 컴파일과 EditMode
  테스트, 그리고 같은 publish tree에 대한 codec 없는 .NET client 설치까지 확인했다.

**파일:** `docs/guide/unity-quickstart.md`, `samples/unity-quickstart/**`,
`unity/GamePatchKit.Unity/Assets/PatchQuickStart.cs`, `README.md`,
`docs/guide/{quickstart,unity}.md`, `samples/README.md`, `.gitignore`,
`docs/plan/13-documentation.md`
