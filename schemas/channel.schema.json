{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "channel.schema.json",
  "title": "GamePatchKit channel pointer",
  "description": "External pointer an environment uses to select the manifestHash and dataVersion a client should target. Not produced by the Packager as part of a release; a publisher creates and updates it separately once a release has been verified and approved.",
  "type": "object",
  "additionalProperties": false,
  "required": ["schemaVersion", "packageId", "manifestHash", "dataVersion"],
  "properties": {
    "schemaVersion": {
      "const": 1
    },
    "packageId": {
      "$ref": "#/$defs/kebabCaseId"
    },
    "manifestHash": {
      "type": "string",
      "pattern": "^[0-9a-f]{64}$"
    },
    "dataVersion": {
      "type": "string",
      "pattern": "^v1-[0-9a-f]{64}$"
    }
  },
  "$defs": {
    "kebabCaseId": {
      "type": "string",
      "pattern": "^[a-z0-9]+(-[a-z0-9]+)*$"
    }
  }
}
